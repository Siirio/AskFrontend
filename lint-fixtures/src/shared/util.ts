// Stub: a legal shared target. The R4 fixture reaches it by a RELATIVE path to
// prove the custom rule bites where boundaries/dependencies (which allows
// slice → shared) stays silent.
export const util = "shared util";
