# ASK Frontend — Changelog

Format: `YYYY-MM-DD | {decision/rationale} | {affected files/features}`

2026-08-02 | **Re-audit against backend `b02105a`: G1 and G3 are both still blocked, B3's plan was refuted by measurement, and four dead paths were found in docs for slices nobody has built yet.** The backend moved two commits past the `cdc47dc` both audits were written on, so every gate was re-read from Java source. **G1 did not move**: `MAX_CANDIDATES` is still 200, `page` still `@Max(20)`, no unique-offers sort, no companies or bbox filter, and the backend's contradicted `Results Filter & Sort V1` lock is still there verbatim. **G3 changed category, not status** — `098fd44` wrote our raise into the backend's OWN ProductVision and added two locks each to `item`/`service` (destinations belong to the entity, never a branch; verification links are never reusable), so the product question is closed on both sides, while `Item.deepLink` is still a single `String`, `Service` still has none, and the search projection still omits them. Their contracts.md states the build order itself. Nothing is left to ask; it is queued backend work. **B3 is the substantive finding.** Both audits carried it as "verify the KATO↔`city` overlap first"; the measurement says the overlap is **0 of 11 954** KATO names against all 23 seeded cities, in both languages — `resolve?name=` would 404 on every call, for every seller. It is not fixable client-side either: KATO holds `с. Караганда` (a village) beside `г. Караганда` (the city), so the obvious prefix-strip files a rural branch under the city silently, which is worse than the honest null it replaces. Converted to a backend raise (resolve by KATO **code** — `KzPlace` already carries it). **`b02105a` also shipped catalog image galleries**, confirmed by the backend developer as "up to 3 images for items and services" and matching `CatalogImageLayout.MAX_IMAGES = 3`: `SearchCardResponse.images` is modelled and stubbed from the Java DTO, and the multipart sync contract is documented for both seller slices — including that it is a SYNC, so omitting a current image deletes it. It is **not rendered**, because the same commit shipped a result-presentation contract (primary image on the row, desktop hover-preview panel, and "match reasons are not displayed") that the vision does not contain and that reverses what we ship — the backend is the authority for DATA, the vision for INTENT, and prose in the other repo is not a licence to invent UI (P9.1/P9.4/D9). Raised for the owner. **N13/N14** found every endpoint in `features/services/contracts.md` and in the cabinet's Unique Offers table pointing at `/api/v1/business-admin/...`, which exists nowhere except `/business-admin/chats`; the real routes are business-scoped `services` and `drops`. Both survived because their slices are unbuilt — the failure mode of a wrong path in a doc for code that does not exist yet. **N1 and N6 closed**: the 30 remaining `AI_Knowledge/` slice numbers hand-checked (11 wrong, 19 already right — the no-blind-replace warning held a third time), and both `LegalController` endpoints documented, since they differ only by the channel they record. | `src/search/model.ts`, `e2e/mock-backend.mjs`, `AI_Knowledge/{ROADMAP,AUDIT_2}.md`, `features/{search,catalog,services,business-cabinet,auth}/contracts.md`, `features/{auth,business-cabinet}/{README,ux-ui-flow,locks}.md`

2026-08-02 | **The city filter could never have worked, and the Unique-Offer tint was reading the wrong thing — one commit, because both were the same root cause: a DTO invented client-side instead of read.** `GET /api/v1/cities` is `CityController.listAll()`: no parameters, whole table, `CityDto {id, name}`. The client sent `?q=<typed>` and read `.city`, so every keystroke re-downloaded the entire city table and rendered it as blank rows with `undefined` React keys, and picking one set the filter to `undefined`. The list is now fetched ONCE, lazily, and filtered in memory — legitimate, because the slice's server-capability lock governs RESULTS, not a picker's own option list. `resolveCity()` was DELETED rather than repaired: it sent `?lat=&lng=` where `name` is required, so it would have 400'd, and nothing called it (P8.1); `business-cabinet` will add a correct name-based call when B3 has a caller. **The tint half was worse than AUDIT_1 recorded.** `separateBadges` treated any UNRECOGNISED token as the offer label, so the first badge the backend ever adds would have rendered raw English inside `bg-offer` — breaking the slice's "unknown badge is DROPPED" lock AND the project's TINT IS INFORMATION lock at once, as a fake discount signal. It was an assignment, so with two unknowns the LAST won, silently overwriting the real label that `resolveBadges()` places first. `hasActiveOffer` is now the authority, and the ordering is a GUARANTEE rather than a guess: the backend computes the flag from `activeOfferLabel` and adds that same label as `badges[0]` inside one call. `knownBadgeKeys()` went with it — dead code (P8.1) that had become a second implementation of the same mapping (P6.1). **Two e2e-stub defects surfaced while writing the tests, both the stub lock in its usual shape:** the card stub put the offer label SECOND with no `has_active_offer` at all, and the mock backend had no CORS — every prior call was server-side during SSR (D7), so the city combobox was the first BROWSER-side call and was simply blocked. Verified: build green, e2e 120/120 | src/search/{api,hooks,model}.ts, src/search/ui/{CityField,ResultCard}.tsx, e2e/{mock-backend.mjs,search.spec.ts}, AI_Knowledge/features/search/contracts.md, AI_Knowledge/{AUDIT_1.md,AUDIT_2.md}

2026-08-02 | **Four live routes stopped looking like a broken build and started saying what is true.** `/app/chats`, `/app/profile`, `/app/business` and `/app/product/:id` rendered a bare `<h1>` plus "Section under construction" inside a neumorphic product — the copy said it, nothing about the presentation did. All four are reachable TODAY, which is what made this worth doing before the slices land: Chats is a permanent nav destination for every user, Settings is one tap inside the account menu, and a seller lands on the cabinet the moment registration completes. They now share `app/_components/SectionNotOpen.tsx` over the `EmptyState` primitive. This is the second of the three endings the "a reachable control must DO something" lock allows, and it is NOT invented UI (P9.1): it is the empty state P9.3 already requires of a surface that exists with no content. **The product route's copy differs on purpose** — the other three are "not open yet", but that one may never open in this form, since D10's deep link is deferred for want of a public item read; it points at the modal instead of promising the URL, and no longer echoes the raw route id, which told the visitor nothing and read as debug output. Server components throughout, so no client boundary was created for a static page (D7). **One test got WEAKER as a side effect and was fixed in the same commit rather than left:** the smoke test asserted a VISIBLE `<h1>`, and these pages moved their heading to `sr-only` for the document outline — an `sr-only` element still has a 1px box, so that assertion would now pass on a blank page. It now checks the heading is attached and that `<main>` is visible and non-empty, plus a new case asserting all four render a visible empty-state | src/app/_components/SectionNotOpen.tsx (new), src/app/app/(main)/{chats,profile,business/(cabinet),product/[id]}/page.tsx, e2e/smoke.spec.ts, src/shared/i18n/messages/*.json, AI_Knowledge/features/{chats,profile,business-cabinet,catalog}/ux-ui-flow.md

2026-08-02 | **Seller registration collects a phone and a business email, so a business stops publishing a card nobody can reach.** Both fields are on `SellerOnboardingRequest` and land DIRECTLY on the business profile, which is exactly what `SearchCardResponse.businessProfile.{number, email}` renders on every result card. Never collecting them meant every business onboarded through this UI shipped a card with no contact channel — silently, with nothing in the interface revealing it — and it starved gate G3, whose surviving candidate was those very fields. Added to step 1, optional as on the backend, DROPPED from the body when blank rather than sent as `""` (the same reason the verification links are), format-checked client-side because the backend applies none at all, and shown on step 5's recap as "Not set" when empty so a seller about to publish with no contact channel sees that first. The format checks are deliberately loose and say so where they live: these become public channels, so a typo is unreachable-for-good, but an over-strict regex rejects real addresses and real numbers on a field nobody must fill. **`contracts.md` carried a doubly-wrong claim that is what kept the gap open** — *"Optional; not collected in V1 (no vision entry — P9.1)"*. P9.1 forbids inventing UI, not filling a field the wire already has; the vision does not enumerate form fields, and a contact channel on a business listing is not a new screen. **Corrected same day after owner review:** the hint shipped on `phone` alone, and in a form where every other field is required, the field WITHOUT a note reads as required — silence inherits the form's default meaning. Both now carry their own hint, each opening with "Optional" and differing only in the verb (call / write), so neither is boilerplate nor depends on the other's position | src/business-cabinet/model.ts, src/business-cabinet/ui/{RegisterStepIdentity,RegisterStepReview}.tsx, e2e/business-register.spec.ts, src/shared/i18n/messages/*.json, AI_Knowledge/features/business-cabinet/{contracts.md,ux-ui-flow.md}

2026-08-02 | **Housekeeping that mostly protects the NEXT agent rather than this build.** Five items, none of which changes behaviour. **N1:** the 2026-07-28 slice renumbering had never reached the code — nine stale roadmap numbers corrected in `src/` against a hand-verified table, while the other nine sites were already accidentally correct and were left alone. That asymmetry is precisely why this could never be a find-and-replace, and the diff was checked to contain comments only. **N2:** `features/catalog/README.md` cited two backend folders that do not exist (`features/catalog/`, `features/import/`); the real modules are `offer/item` and `importing`. CLAUDE.md sends every agent to the backend contracts before consuming an endpoint, so this pointed whoever builds slice #3 down a dead path — and `contracts.md` in the same folder was already right, so only the README line was wrong. **D-5's residual:** `prettier` and `prettier-plugin-tailwindcss` pinned exactly, because a caret range on a FORMATTER makes "formatted" a moving target on a CI-gated check, which is how the original ten-file drift appeared. **D-6:** `pinLocale` lifted to `e2e/helpers.ts`, carrying its corrected rationale — a different port never dropped the cookie, since cookies are keyed on host, not port. **N10:** a `globalSetup` now REFUSES the run when the harness is attached to a dev server, detected by markup only development emits. `playwright.config.ts` promises it drives the production build and then sets `reuseExistingServer` locally, so a dev server on `:3000` was silently graded instead — green, and invisible, because CI sets the flag false and never sees it. Proven both ways: it fails against a running dev server and stays silent against `next start`. **N3 was deliberately NOT actioned and its entry corrected instead** — its premise was half wrong (16px is used three times in `neumorphism.css`, so the toast matches the skin and its comment is true), and the real finding is that the skin declares four radius tokens then uses 9/10/11/16/18/20px literals, which is a platform-wide visual change belonging in the UI work loop | src/app/app/(main)/**, src/business-cabinet/**, e2e/{helpers.ts,global-setup.ts} (new), playwright.config.ts, package.json, AI_Knowledge/{AUDIT_2.md,features/catalog/README.md}

2026-08-02 | **G1 and G3 both ANSWERED by the owner, and both convert from product questions into backend dependencies — G1's harder than it was.** **G1: filtering and sorting run on the SERVER across the whole catalogue, always, and pagination is replaced by INFINITE SCROLL on every viewport.** Changing a filter or sort discards the loaded results and re-queries from page 0. This is none of the three options the roadmap offered — it is stricter than option 2 and the reverse of option 1 — so `features/search/locks.md`'s server-capability lock is REAFFIRMED rather than rewritten, and the backend prose that described client-side refinement is simply overruled. **Three consequences, and the second one blocks the item.** (1) The three parked controls (Unique-Offers sort, Companies filter, map-area) now REQUIRE real server parameters: a client-side refinement layer was the fallback and the decision forbids it, so a "nice to have" became a hard dependency. (2) **`MAX_CANDIDATES = 200`** (`StructuredSearchProcessor:49`) caps ranking at 200 candidates for ANY page, and `page` is `@Max(20)` besides — so "scroll until the goods run out" dies at roughly 200 results however many items match. Infinite scroll is unbuildable until that changes; it needs a deep-paging strategy, not a bigger constant. (3) `search/` gains a `store.ts`, reversing `hooks.ts`'s "nothing here needs to outlive one render" premise — legitimate, because the store accumulates honest server pages and a new lock forbids it from ever filtering them. **A detail worth keeping: the backend's `Results Filter & Sort V1` lock contradicted its own code before it contradicted us** — `SearchFilterRequest` already implements server-side category/city/country/minPrice/maxPrice/openNow/radiusMeters with a cross-field assert, while the lock beside it says refinement only reorders loaded cards. **G3: "Proceed to Purchase" opens seller-supplied public DEEPLINKS — a chooser modal when there is more than one, the in-app chat with an editable, never-auto-sent draft when there are none.** Multiple deeplinks were explicitly approved over a one-per-item reading: a brand that sells in several places must not have that collapsed into a channel we picked for it. This keeps the two buttons distinct, which the vision requires by giving the card a separate chat button — chat asks a question, Proceed states intent to buy. **Verification links are NEVER reused as customer deeplinks**: `SellerOnboardingProcessor:64` writes `kaspiUrl`/`ozonUrl`/`wildberriesUrl` to `BusinessVerification`, i.e. they were collected as proof the business is real, and re-purposing them would use data for something other than the reason it was given — found independently in this repo before the owner stated the same rule. **G3 is now backend-blocked in three places:** `Item.deepLink` is a single `@Size(max=2048) String` and must become label+URL pairs or the modal renders bare URLs; **`Service` has no `deepLink` at all**; and `SearchDocument` does not carry it, so it never reaches `SearchCardResponse` and the Product Card cannot see it. **The one sub-question this entry originally left open — per ITEM or per BRANCH — was answered the same day: per ITEM/SERVICE, never per branch.** A branch is a physical place, and the only link that would ever hang off one is a map/location link, which is a different concept and not this button. So the backend shape is a list of label+URL pairs on the item, with no branch foreign key and no per-branch variants, and "several deeplinks" means several places to buy the SAME item — exactly what the chooser modal is for, never "one link per shop". Recorded here rather than left in the chat, because the distinction is the kind that reads as obvious once stated and gets silently re-litigated otherwise. Until the field lands the button is OMITTED, not disabled — a reachable control must DO something | AI_Knowledge/PRODUCT_VISION.md (two append-only blocks: UF 2.1 "Proceed to Purchase", §4 "How results page, filter and sort"), AI_Knowledge/ROADMAP.md (G1/G3 gate rows + G1 resolution + 3 new Cross-Repo rows), AI_Knowledge/features/search/locks.md (+2 locks), AI_Knowledge/features/catalog/{README.md,ux-ui-flow.md}, AI_Knowledge/AUDIT_2.md (owner table + Wave 0)

2026-08-02 | **The two crawlable `/app` pages get the `noindex` the gated ones already had — and the false sentence that inverted it is corrected in three places (AUDIT_2 N9).** `/app/auth/login` and `/app/auth/register` shipped with no `robots` tag while `/app/business/register`, which `RequireAuth` makes unreachable, carried one justified by *"the whole `/app/*` tree is authenticated — there is nothing here for a crawler."* That sentence is false for exactly one subtree, and `Locks.md` states the exception in the same breath as the rule it comes from: **`/app/auth/*` is the ONLY logged-out-reachable `/app` subtree** (D23). The generalisation was applied to a page it did not cover and skipped on the two pages it did, so the protected route was the unreachable one. **The tag is two lines; the sentence is the fix.** It originates in ROADMAP item 10, where it justifies skipping SEO work for all of `/app/*` — left standing, whoever writes `robots.ts` would read it, believe the gate does the work, and re-expose both pages. Corrected in the roadmap (dated note), in `business/register/page.tsx` (its `noindex` reason is now *placement* — `RequireAuth` on that route — which matches how that file already explains its access decision, rather than a claim about the tree), and in `features/auth/ux-ui-flow.md`. **Item 10 still owes `robots.ts` with an explicit `Disallow: /app/`**: per-route tags cover only the pages that exist today, and the next logged-out-reachable `/app` page (a public storefront, a shared link) would otherwise ship indexed with nothing in the tree to catch it. **Guarded in e2e**, following `design-system.spec.ts` and `verify:rendering`: the failure mode is invisible — no error, no visual difference, discovered by finding "Log in — Ask" in a search result — so a silent removal has to fail the suite rather than the launch. Low severity throughout (thin content, no authenticated data exposed, nothing deployed yet), recorded at length only because it is a **reasoning** defect: a true general rule, an exception written down beside it, and a comment that quoted the rule without the exception. **This closes the last of `auth`'s 8 DONE criteria** | src/app/app/auth/{login,register}/page.tsx, src/app/app/(main)/business/register/page.tsx (comment), e2e/auth.spec.ts, AI_Knowledge/{ROADMAP.md (item 10 + dated note),AUDIT_2.md (N9 closed),features/auth/ux-ui-flow.md}

2026-08-01 | **Review follow-up on the consent commit: A1 RE-OPENED, four stale doc claims corrected, and a proposed runtime block converted into a lock.** A review of `c307730` raised six findings; five were right, one was not, and the most valuable was the one that said **do not close A1 while consent persistence is best-effort.** It was correct, and the evidence was A1's own opening text: *"a network failure toasts and proceeds — so registration can complete with no record."* A1 had TWO halves. The COVERAGE half is genuinely closed (every registration path records, and only documents the user was shown). The **PERSISTENCE** half was never touched — no retry, no queue, no replay on the next session — and marking the item `[x]` would have retired the finding that records it. Re-opened, with the remaining question stated as the PRODUCT decision it is: blocking registration on a failed legal write strands someone with a valid account, proceeding silently is where we are, and retry/replay are plausible answers nobody has chosen. **Narrowed, not closed:** the OAuth path was fire-and-forget (`void`) on the reasoning that a transient redirect page has nothing to hold — but the redirect was exactly what the write raced. Now `await`ed like the verify step, so on both paths the call has completed or toasted before the user leaves. That removes the race; it does not make the write durable, and the entry says so. **Four stale doc claims** the earlier `src/` grep did not reach, because they were prose rather than identifiers: `features/auth/README.md` carried SIX (two `startRoute` landings, "frontend not yet built" for shipped OAuth, "answering customer also records legal consent" — the exact defect A1 fixed — plus seller-registration listed as deferred when it shipped 2026-07-27, and two pre-renumbering slice numbers), and `ux-ui-flow.md` still timed consent at the role answer. **The e2e assertion was weak:** it counted two links in the consent line without checking where they point, which would pass if both went to the same page or to neither; it now asserts `/terms` and `/privacy` by href, since consent is only meaningful if it names the documents it links. **The one finding declined:** "block this flow if the linked legal documents are still placeholders." There is no wire signal for "this document is a placeholder", so the client would hardcode a flag (P7.3 — a mechanism invented for a release concern); it would break sign-in wherever copy is not final; and it would have to gate the email form's checkbox identically or the product would hold two consent standards for the same two documents. The concern is real and the control is RELEASE-side, so it became a **Product Lock** instead — consent is never collected in production against placeholder legal copy — which makes skipping ROADMAP launch item 11 a STOP rather than an oversight. **The generalisable bit: a review that says "your doc is stale" is usually cheap to dismiss and usually right — five of six here were, and the two biggest were things a grep for identifiers could never find** | src/auth/hooks.ts (awaited OAuth consent), e2e/auth.spec.ts (link destinations), AI_Knowledge/features/auth/{README.md,ux-ui-flow.md}, AI_Knowledge/{AUDIT_1.md (A1 re-opened),Locks.md (placeholder-consent lock)}

2026-08-01 | **P1.1's scope is written down (new P1.1a), and the one file it actually indicts is split (owner decision).** Two questions that looked like one. **(a) The rule.** `P1.1` says *"a component file MUST NOT exceed ~400 lines"*, and it was applied to slice `hooks.ts`/`model.ts` twice — AUDIT_1 B8, then the 2026-08-01 audit — and actioned NEITHER time. The second pass also showed the finding was not even self-consistent: it named `business-cabinet/model.ts` (465) and `hooks.ts` (412) while `auth/hooks.ts` (607) and `auth/model.ts` (421) went unmentioned, both larger. **P1.1a** now states the scope: the cap is about COMPONENT files; a slice's `api.ts`/`model.ts`/`hooks.ts` is a module whose length tracks the DOMAIN's surface, governed by P1.3 (one responsibility per hook) and P1.4 (do not over-split) instead. The alternative — amending architecture §3 to admit a `hooks/` folder — was considered and rejected: §3's fixed anatomy is a deliberate anti-drift device *because* this codebase is written mostly by agents, and loosening it for `hooks/` invites `api/`, `model/`, `store/` next. **The timing was the point:** decided before slices #3–#6 were written, so four more slices inherit a settled shape rather than the same argument four more times. A rule that keeps producing unactioned findings is worse than no rule — it trains readers to skim the list. **(b) The actual violation.** `app/_components/NavigationMenu.tsx` was **523 lines** — a real component, holding the destinations, the desktop dropdown, the mobile sheet and a GSAP FLIP tween — and it appears nowhere in AUDIT_1. Split into `NavigationMenu` (**188**), `AccountMenu` (**290**) and `useNavIndicator` (**93**). **No rule changed, and that asymmetry is the whole reason this one could just be fixed:** `app/_components/` has no fixed anatomy — §2 only says it holds app chrome — so adding files there is a pure refactor. The seam is the axis that does NOT change together: destinations vs. account. The two account PRESENTATIONS stay in one file on purpose (a new menu row must be added to both — P1.4), and `AccountMenu` reads the session itself via `useAuth` (R6) rather than taking six props, which also moved `signOut` + the router to the account concern where they belong. `initialsFrom` was carried over byte-for-byte — an in-flight "improvement" (adding `.trim()`) was reverted, because a refactor that quietly changes behaviour is not a refactor. Verified: `eslint src`, `tsc --noEmit`, `prettier --check` clean; `navigation.spec.ts` **6/6 on chromium AND 6/6 on mobile-chromium**. **That mobile run closed AUDIT_1 D-7 as already-fixed** — commit `c322331` had repaired it hours earlier and the box was never ticked, the same staleness as D-5, and the third unchecked item this week found to be green | src/app/_components/{NavigationMenu.tsx,AccountMenu.tsx (new),useNavIndicator.ts (new)}, AI_Knowledge/DESIGN_PATTERNS_FRONTEND.md (P1.1a), AI_Knowledge/AUDIT_1.md (B8 resolved-as-not-a-finding, D-7 closed)

2026-08-01 | **The Google button gets consent copy, which is what makes recording a Google sign-up's legal consent honest (owner directive; closes AUDIT_1 A1's residual).** A first-time Google sign-up accepted no agreement anywhere: the OAuth buttons presented none, so `useOAuthCallback` deliberately recorded nothing — the gap was raised in contracts.md rather than papered over (P9.4), because sending `documentCodes` for text a person was never shown is a worse defect than the missing record. **Fixed at the cause:** `OAuthOptions` now renders a consent line beside the button — the same two documents, the same wording, the same `/terms` and `/privacy` links as the email form's checkbox — and `useOAuthCallback` records `USER_TERMS` + `PRIVACY_POLICY` when the callback carries `?registration=1`. **The non-obvious half is that the copy renders on the LOG-IN page too.** Google sign-in REGISTERS: `CustomOAuth2UserService` computes `registrationRequired = user == null` and then calls `identityService.createUser(...)`, so an unknown email creates an account from Log in exactly as from Sign up — read from the Java, not assumed. Copy under Sign up alone would have left the more common door consenting to nothing, and the defect would have looked fixed. **Passive consent, not a checkbox:** the click IS the agreement (there is no form to gate — the control is an `<a>` that leaves the origin), and a checkbox in front of a link would be a second consent grammar for the same two documents. The email form keeps its checkbox, which gates a form it can actually block. Both paths now call ONE `recordRegistrationConsent(locale, t)` — the single place that knows which documents a registration accepts and that a failure toasts rather than throws (P6.2), with no caller-type flag (P6.3). It is awaited on the verify screen (there is a screen to hold) and fire-and-forget on the callback (a transient redirect must not wait on a best-effort legal write; the helper never rejects, so no unhandled rejection). Copy is a full sentence the system speaks, so it takes a terminal period in all three locales (P9.5), and the document NAMES match `fields.agreement` word for word in ru/kk/en — same documents must read the same. **Still true and still the launch blocker:** the Terms/Privacy bodies are placeholders (launch item 11) — recording consent against placeholder text is what must not reach production | src/auth/ui/OAuthOptions.tsx, src/auth/hooks.ts (recordRegistrationConsent, useOAuthCallback, useVerifyStep), src/shared/i18n/messages/{en,ru,kk}.json (`auth.oauth.consent`), AI_Knowledge/features/auth/{contracts.md,ux-ui-flow.md}, AI_Knowledge/{AUDIT_1.md (A1 residual closed),ROADMAP.md}

2026-08-01 | **Post-auth routing is a CONSTANT, not a resolved value — two route mappers deleted, an auth lock retired, and AUDIT_1's A7 withdrawn as a misdiagnosis (owner correction).** `AuthProcessor.resolveStartRoute()` and `LoginProcessor.resolveStartRoute()` both `return "CLIENT_SEARCH";` for every account. Two audit passes read that as a backend regression — a resolver someone had gutted — and built policy on it: A7 filed a cross-repo dependency asking the backend to "resolve `startRoute` from memberships again", `startRouteToPath` kept `OWNER_BRANCHES`/`BRANCH_WORKSPACE` arms "so a future backend change needs no client edit", and an auth lock asserted *"startRoute from the backend decides the post-login route — the client never hardcodes it."* **All of it rested on never opening PRODUCT_VISION.** UF 1 step 3 reads `Home + Role Choosing Modal`, for every role. The constant IS the product decision; the backend was correct the whole time. **The inversion is the lesson: the SHAPE of code was taken as evidence about the product, which is exactly what D9/P9.1 exist to prevent** — and it cost a phantom cross-repo item, ~40 lines of speculative branching, and a lock that made a correct backend look delinquent to every subsequent reader. **Closed by deletion (P8.2, P7.4), not by a patch.** `startRouteToPath` (auth) and `onboardingStartRouteToPath` (business-cabinet) are gone — the second was a switch whose `MANAGED_IMPORT`, `BUSINESS_CABINET` and `default` arms all returned the same string, a decision with one answer, kept explicit "so the day the other screen lands this is the one line that changes"; writing that branch at roadmap #7 costs the same then, minus the years of reading a switch that cannot switch. Replaced by two named constants carrying the rule: **`POST_AUTH_PATH` = `/app`** (login, verify, OAuth callback alike) and **`POST_ONBOARDING_PATH` = `/app/business`** (D26, the one flow that deliberately lands elsewhere). `startRoute` stays on both response types — it is on the wire, and modelling a field you receive is honest; branching on a field with one value is not. **No behaviour changed on any path**: the role modal's business answer already routed to `/app/business/register`, `RequireDashboardAccess` already bounced there (D27), onboarding already landed on the cabinet, everyone else already landed on Home. What changed is that the code stops implying a gap that does not exist. The auth lock is RETIRED rather than deleted (its own convention) with the reasoning kept, since *"a lock asserting who OWNS a decision is only useful while the decision has more than one possible answer"* generalises past this case. e2e stub VALUES are unchanged and correct; their comments are reworded, since justifying `CLIENT_SEARCH` by "no backend path produces anything else" describes the symptom and invites someone to go fix the backend | src/auth/{model.ts,hooks.ts}, src/business-cabinet/{model.ts,hooks.ts}, e2e/{business-register,guard,navigation,smoke}.spec.ts, AI_Knowledge/features/auth/{locks.md (retired),README.md,contracts.md,ux-ui-flow.md}, AI_Knowledge/features/business-cabinet/{contracts.md,ux-ui-flow.md}, AI_Knowledge/AUDIT_1.md (A7 closed, A8 amended)

2026-08-01 | **`check-tokens.mjs` was shipping the defect it exists to prevent — a false green on alpha-bearing tokens (review, same day it landed).** Its token regex demanded exactly three numbers, so `oklch(0 0 0 / 0.45)` never matched: the three shadow colours and `--overlay` were silently absent from a drift check whose own output claims to compare "token by token" — **21 of 24 tokens per block**. Proven by demonstration, not argument: drifting `--shadow-color` from `/ 0.45` to `/ 0.99` in the no-JS block printed `PROVEN  the no-JS fallback mirrors the dark palette exactly`. A comment in the file even *stated* the exclusion, but justified it for the CONTRAST table (a translucent token has no fixed pair — true) while the same parse silently fed the DRIFT check, where the reasoning does not hold at all. **Fixes:** alpha is parsed (defaulting to 1) and compared alongside L/C/H; the drift message prints alpha whenever either side carries it, since a pure-alpha drift would otherwise render as two identical-looking values; and `rgbOf` now REFUSES a translucent token as one side of a documented pair instead of measuring it as if opaque — compositing is not modelled, and a confident wrong ratio is worse than a stop. **Second finding, nitpick:** `PAIRS` keyed on the raw table label, coupling semantic identity to column ALIGNMENT — deleting one padding space reported the same row as both "unknown to this script" and "absent from the table", two alarming errors for a reformat that changed no meaning. Keys now match on collapsed whitespace. **A broken TEST was also caught while re-verifying:** the opaque-drift case targeted line 253 (`--foreground-subtle`) with a pattern only line 251 (`--foreground`) could match, so the `sed` was a no-op and the case reported "0 BROKEN" — the check was fine, the test proved nothing. Re-run against the right line, it fails correctly. All four defect classes are now demonstrated failing: a ratio that no longer matches its documented number, a miscounted summary line, an opaque drift, an alpha drift. **The durable lesson: a check is only as good as its parser, and "it passes" is not evidence — every class it claims to catch has to be shown failing** | scripts/check-tokens.mjs, AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md (§8 note)

2026-08-01 | **The measurement lock gains enforcement: `scripts/check-tokens.mjs` recomputes the whole WCAG table on every build.** Two review findings on `tokens.css`, both nitpick-severity, both symptoms of the same gap — the lock says every rendered colour pair is measured and written down, but nothing re-derived those numbers after 2026-07, so the table was a claim the build never tested. **(1) The count was never reconcilable.** The summary read *"All 31 measured pairs pass"* against a table of 13 rows × 2 themes = 26, and had done since the table was written (`f91b558`) — the table was never longer, so 31 was wrong from the first commit rather than left behind by a deletion. **(2) The no-JS dark fallback was protected by prose only.** `@media (prefers-color-scheme: dark) :root:not([data-theme])` repeats the dark palette verbatim, held in step by a comment saying "change the two dark blocks together" — drift invisible in review, invisible in a diff, and reachable only by a visitor with scripts blocked, i.e. the one audience that could see it and the one that cannot report it. **All 26 pairs were re-derived before anything was edited** (OKLCH → OKLab → LMS → linear sRGB → WCAG 2.x luminance) and **every one reproduces the committed number exactly**, with 0 failures and 0 out-of-gamut values — so the table was right and only its count was wrong. That check-first order mattered: 11 of 13 rows matched on the first run and 2 did not, which turned out to be an ambiguous LABEL rather than a bad value (`accent:hover / foreground` pairs the hover fill with `--accent-foreground`, the ink that sits on it, not the page's `--foreground`) — had the numbers been "corrected" to match a wrong reading, two verified ratios would have been destroyed. The mapping now lives executably in the script's `PAIRS`, so it cannot be re-guessed. The check asserts five things: table and script cover the same pairs, every ratio recomputes, every pair clears its floor, the count derives from the table rather than the prose, every token is in sRGB gamut, and the two dark blocks are identical token by token. Wired BEFORE `next build` (it reads source, not build output) and proven both ways like its siblings — green on the real file, exit 1 on each defect class: a drifted no-JS value, a token whose ratio no longer matches, a miscounted summary. **Not covered:** `neumorphism.css`'s own table, whose values are hex rather than OKLCH, so the parser does not reach them — stated on the lock rather than left to be discovered | scripts/check-tokens.mjs (new), package.json (`verify:tokens`, build chain), src/design-system/tokens.css (count + two §notes), AI_Knowledge/{Locks.md, ARCHITECTURE_PATTERN_FRONTEND.md §8 note}

2026-08-01 | **The `*_old` archive and the `/demo` design lab DELETED; `tokens_old.css` renamed back to `tokens.css` (owner directive, D31).** This reverses the 2026-07-27 directive *"do not remove old styles, leave them but rename everything as `..._old`"* and retires **D24**; the D25 skin itself, its `.neu-skin` scoping mechanism and every measured value are untouched. **30 files removed:** 26 `*_old.tsx` (~3,000 lines — 12 in `auth/ui`, 13 in `shared/ui`, `NavigationMenu_old`) and `src/app/demo/` (`page.tsx`, `DemoLab.tsx`, `LabComponents.tsx`, `neumor.css`). **Why the archive stopped earning its place:** it lived inside `src/`, so it type-checked, linted and answered every grep on every build (AUDIT_1 item D-4), while git already held the same content in a form that restores as a coherent whole. Relocating it outside `src/` — D-4's own suggestion — was considered and rejected, because the set no longer composed as a drop-in anyway: `globals.css` removed `focus-ring-field` at D25, so `input_old`/`select_old` would render focus-less, a fact the file had to *document* rather than fix. Restoring the retired skin is a `git revert`, which brings the utility back with it; a file swap never could. **Why /demo went too:** its job was to hold the unmeasured neumorui specimen for comparison against the shipped values. That comparison concluded at D25 and its output is written down — the three recorded darkenings and the WCAG table in `neumorphism.css` — which is the durable form of a specimen. Nothing linked to `/demo`; it was an orphan route. **The rename is the load-bearing part of this change.** `tokens.css` was swept into `tokens_old.css` at D25 although it never stopped being imported and still owns the ONE `@theme inline` block for the repo (every `bg-surface`, `text-sm`, `rounded-sm`, `ease-out` resolves through it, neumorphic surfaces included) plus the live palette for the marketing landing. A filename asserting the opposite of the truth cost enough that AUDIT_1 carried D-4 partly to warn the next reader not to believe it — and the finding still miscounted the archive at 27 files by including this CSS file in it, which is the misdirection demonstrating itself. Deleting the real archive removed the last reason to keep the misnomer. **Two Product Locks retired, not deleted** (the `/demo` placement lock keeps its reasoning, since it is the general rule any future lab would be rebuilt against; re-opening a lab is a NEW decision, as D24 is retired rather than dormant), and the vision lock's ONE exemption is gone — every surface in the repo must now exist in `PRODUCT_VISION.md` on its own account, so this tightens the lock rather than loosening it. **Costs, accepted and stated:** the product has no design lab, so the next visual exploration needs a home agreed before it starts (a scratch branch being the cheap answer). Verified: `tsc --noEmit`, `eslint src`, `prettier --check .` all clean, and `npm run lint:fixtures` still proves R1–R5 | src/app/demo/* (deleted), src/{auth,shared}/ui/*_old.tsx + src/app/_components/NavigationMenu_old.tsx (deleted), src/design-system/tokens_old.css → tokens.css (renamed), src/app/{globals.css,layout.tsx}, src/design-system/neumorphism.css, src/shared/{motion,theme}.ts, e2e/design-system.spec.ts, AI_Knowledge/{Locks.md,ARCHITECTURE_PATTERN_FRONTEND.md §2 + §11 (D31, D24 retired),ROADMAP.md,AUDIT_1.md (D-4 closed)}, .claude/skills/{marketing,platform}-ui-design/SKILL.md

2026-08-01 | **AUDIT_0 retired — every item resolved or consciously accepted; the file is folded
here per its own rule.** Its full record: **Finding 1 (D6 static-landing lock violated)** —
fixed 2026-07-18 with `setRequestLocale(defaultLocale)` at both static entry points, and guarded
permanently by `scripts/check-rendering.mjs`, still a mandatory stage of `npm run build`
(re-verified 2026-08-01), so a dropped locale seed fails the build instead of silently reverting.
**Finding 2 (`toAuthUser` silently degrades a business session)** — RESOLVED 2026-08-01, though
not as written: the 2026-07-28 rewrite had already made the described mechanism obsolete, and the
real remaining risk was the opposite of a degrade — substring matching would have granted full
business access to any role merely CONTAINING `OWNER` (`CO_OWNER`, `FORMER_OWNER`). Now exact
enum matching, `null` for unknown, `hasUnknownMemberRole()` exported. No alert built: the backend
emits only OWNER/MANAGER/WORKER so the case cannot fire (P8.2). **Finding 3
(`INVALID_CREDENTIALS` mapped twice)** — ACCEPTED, no action, re-verified 2026-08-01: the inline
branch also handles a bare `401`, which a code-keyed map cannot express, both live in one file,
and the duplication cannot spread. **Finding 4 (unused `AuthSessionResponse` fields)** — RESOLVED
2026-08-01 by COMPLETING the mirror rather than trimming it; the type called itself "the full
session contract" while missing `expiresIn` and the four `SessionCapabilitiesProcessor` fields.
**Finding 5 (Dialog's `max-w-[calc(100%-2rem)]` literal)** — ACCEPTED 2026-08-01, and the reason
overturns the finding: `tokens_old.css` uses `@theme inline`, which compiles spacing to LITERAL
values and resolves only colours through `var()`. There is therefore no runtime `--spacing`
custom property to reference, and the "obvious" token swap would emit an invalid `calc()`, drop
`max-width` entirely and make every modal full-bleed on mobile. The literal is the correct
expression for this theme, not a shortcut; it is contained in one primitive and cannot
propagate. | AI_Knowledge/AUDIT_0.md (deleted), src/auth/model.ts, src/shared/ui/dialog.tsx
(unchanged, decision recorded), scripts/check-rendering.mjs

2026-08-01 | **Auth audit closed out — nine findings, and the two sharpest were both
"the code trusted a value the backend stopped producing".** (1) **Registration consent moved
from `RoleSelectionModal` to `useVerifyStep`.** Binding it to a ROLE ANSWER meant anyone who
chose "business" never had Terms/Privacy recorded at all (the seller flow records
`SELLER_TERMS`, never those two), and — worse — the modal also opens for first-time GOOGLE
sign-ups, which present no agreement checkbox, so the client was recording consent for text
the person was never shown. The consent belongs to registration, not to a role. A Google
sign-up now records NOTHING and that gap is raised, not faked (P9.4). (2) **`startRoute` is a
hardcoded constant server-side** — `AuthProcessor.resolveStartRoute()` and
`LoginProcessor.resolveStartRoute()` both `return "CLIENT_SEARCH"` — so the seller-onboarding
flow, which routed off the refreshed session, sent every newly-registered seller to Home; its
`/app/business` fallback only survived when the session refresh THREW, meaning the failure
path routed correctly and the success path did not. Onboarding now routes from
`SellerOnboardingResponse.startRoute`, the one value on the wire that still means something,
and `useRefreshSession` returns `void` rather than a route that was always `/app`. Login-time
routing was NOT changed and is not treated as a defect: nothing in PRODUCT_VISION says an owner
lands on the cabinet at sign-in, and the nav's Dashboard link is the way in. (3) Register body: dropped
`acceptedUserAgreement` (not a field on the DTO — silently discarded by Jackson) and started
sending `countryCode`/`locale`, so accounts stop being stamped `"ru"` in a product whose
default locale is `kk`. (4) `roleToKind` matches the backend enum EXACTLY instead of
substring-matching — the old test would have granted full business access to any value merely
CONTAINING `OWNER` (`CO_OWNER`, `FORMER_OWNER`), an escalation rather than a degrade — and
returns `null` for an unrecognised role. No user-facing alert was added: the backend emits only
OWNER/MANAGER/WORKER, so the case cannot fire, and building for it would be pre-building (P8.2). (5) `AuthSessionResponse` completed: it called itself "the full
session contract" while missing `expiresIn` and the four `SessionCapabilitiesProcessor`
fields. (6) `GET /session` returns a REAL freshly-issued token, not null as api.ts and
contracts.md both claimed — which means every session restore silently rolls the token
forward. Documented, not changed. (7) Four e2e stubs answered `start_route: "OWNER_BRANCHES"`,
a value no backend path emits — the e2e-stub lock again. | src/auth/{model.ts,hooks.ts,api.ts},
src/auth/ui/RoleSelectionModal.tsx, src/business-cabinet/{model.ts,hooks.ts},
e2e/{business-register,guard,navigation,smoke}.spec.ts, features/auth/contracts.md,
ROADMAP.md, AUDIT_0.md, AUDIT_1.md

2026-08-01 | Fixed a skin-portal race that only Google OAuth's first-signup path could
trigger: `useSkinPortalContainer`'s `useState` lazy initializer runs during React's render
phase, before the commit that creates `#ask-skin-root` touches the real DOM, so a portaled
Dialog that mounts ALREADY OPEN in that same commit (the role modal, armed before a
client-side navigation into `/app` from `/oauth/callback` — a route outside
`app/app/layout.tsx` entirely) always found nothing and rendered with no backdrop and no
card background. The email/password path never showed it because its auth pages already sit
inside `app/app/layout.tsx`, so the wrapper predates the modal opening. Added a
`useLayoutEffect` re-check, which runs after DOM mutations commit but before paint, so it
reliably finds a sibling/ancestor created in the same commit with no visible flash. Caught
driving a real Google sign-up end-to-end, not by reading the code | src/shared/ui/skin-portal.ts

2026-07-31 | **Code-review fixes on the D30 address cascade — one of them a real data
defect.** (1) The cascade, the map pin and the street line are three descriptions of ONE
location and only the first could be changed: picking a new region left the old coordinates
and street attached, so a branch could be submitted claiming one oblast and pointing at
another. Changing the place now clears the pin, street and search box — keyed on `kzPlaceKey`
(ids only) so switching LANGUAGE, which re-emits the same place with re-rendered names, does
not throw away a dropped pin. The modal's field order was inverted to match
(name → cascade → search/map/street): with the map on top that reset would have destroyed
work the seller had already done, and the new order is the order an address is written in.
Containment is deliberately NOT verified — KATO has no geometry — but Nominatim searches are
biased by the chosen place. (2) `loadLocalities` now REJECTS instead of resolving to an empty
map: an empty chunk is indistinguishable from a district with no settlements, so a dropped
chunk silently turned "we could not load them" into "there are none", and the address was
reported complete at district precision with a question missing. The failure is now a state
of its own with a retry beside the field. (3) `complete` is no longer emitted from the click
handlers — it depends on the lazily-loaded chunk and on the locale, neither known at click
time, so a district picked mid-flight was reported complete and a later language switch left
the caller holding stale names. Derived value, ref-guarded emit effect. (4) Combobox: clear
the blur timer on unmount (the cascade unmounts whole levels), and scroll the active option
into view during keyboard navigation — these lists reach 373 rows. (5) Nominatim: 8 s
deadline and `accept-language`. | src/shared/ui/address-select.tsx,
src/shared/ui/combobox.tsx, src/shared/geo/kato/index.ts, src/business-cabinet/api.ts,
src/business-cabinet/ui/BranchMapModal.tsx, src/shared/i18n/messages/{ru,kk,en}.json,
e2e/business-register.spec.ts, features/business-cabinet/contracts.md,
features/business-cabinet/ux-ui-flow.md

2026-07-31 | **KATO address cascade ported in as a shared control (D30).** Adapted from another
project's Vue/PrimeVue `AddressSelect`. Three judgement calls, all recorded in D30: `shared/geo/`
opened for reference data (KATO names oblasts and settlements, no domain concept — the §5 litmus
test, same tenancy as `i18n/messages`); the 1.9 MB export split by `scripts/build-kato.mjs` into
24 KB eager + 17 lazily-imported locality chunks, so 1.1 MB of settlements never enters a
registration form's bundle; and **Kazakhstan only** — the port's worldwide country select was
dropped rather than carried, because `REGISTRATION_COUNTRY_CODE` is fixed to KZ and
`BusinessLegalForm` is KZ-specific, making a country picker a one-answer dead control (project
lock) and its `country-state-city` dependency dead weight (a second market is **G4**). Two
deviations from the port, both to fix real defects rather than taste: the free-text street input
was taken OUT of the control (bundling it made the component half-controlled the moment a caller
also wrote that string — which `BranchMapModal` does, from the map pin), and the city-first
ordering of the merged district/city list moved to render time because `localeCompare` orders
Kazakh's ә/ғ/қ/ң/ө/ұ/ү/һ/і differently from Russian, so a build-time order can only be right for
one language. `reverseGeocode` narrowed to road + house number for the same reason the cascade
exists: OSM's `display_name` would repeat three levels the seller already answered, in a
transliteration that need not match theirs. Composed into the ONE `address` string
`CreateBranchRequest` has — no invented DTO field (P9.4); `cityId` remains unsent (AUDIT_1 B3) |
scripts/build-kato.mjs, src/shared/geo/kato/ (index.ts + generated regions/districts/localities
JSON), src/shared/ui/address-select.tsx, src/shared/ui/combobox.tsx, src/business-cabinet/api.ts,
src/business-cabinet/ui/BranchMapModal.tsx, src/shared/i18n/messages/{ru,kk,en}.json,
e2e/business-register.spec.ts, .prettierignore, ARCHITECTURE_PATTERN_FRONTEND.md §2 + §11 D30,
features/business-cabinet/contracts.md, features/business-cabinet/ux-ui-flow.md

2026-07-30 | Closed a batch of auth/identity gaps found by re-auditing against backend `dev@2e06cbe`
(4 commits ahead of the previous audit's `9a90f5c`): removed `requiresRoleSelection`/`availableRoles`/
`RoleOption`/`suggestRoleExpansion` (the backend deleted the concept outright); armed the Google
OAuth first-signup role modal via the callback's new `?registration=1` query param instead of the
dead session field; modelled `isTwoFactorEnabled` and the `AuthBusinessContextResponse` category/
scope fields the backend added; added `phone` to `AuthUserResponse` (code now matches the doc fix
from 2026-07-29); wired `POST /api/v1/legal/registration-acceptances` from `RoleSelectionModal`
for the customer answer only (business answer is `business-cabinet`'s gap, flagged not fixed);
corrected the auth contracts.md endpoint table (email-change/password-change/two-factor
request+confirm, not the five routes it used to name); dropped the dead `rememberMe` field. The
OAuth redirect port default and the `app_user.email` uniqueness gap were raised in the same audit
but are explicitly out of scope (owner call) | src/auth/*, e2e/auth.spec.ts, features/auth/*

2026-07-29 | **Step 2's catalog-setup cards, screenshotted by the owner, had two real bugs, not
just a rough edge — fixed and re-verified with a Playwright screenshot before reporting done.**
(1) `items-stretch` + `h-full` + `justify-center` forced both cards to the same height, and
since the two cards' content lengths are wildly different (one line vs. reassurance + price +
note), the short "Add manually" card ended up mostly empty with its content vertically centered
in a huge dead void — reads as broken, not "balanced." (2) The selection dot was inline beside
the title in a flex row; the moment the managed-import title wrapped to two lines, the dot got
pushed up and OUT of the card, overlapping its rounded corner. Fixed by (a) making the dot an
absolutely-positioned corner element independent of the title's line count, and (b) — after
seeing the fix still look unbalanced side-by-side — stacking both cards full-width in ONE
column instead of a 2-column grid, per owner feedback; a short card next to a tall one never
reads as intentional regardless of internal alignment | src/business-cabinet/ui/RegisterStepScope.tsx,
AI_Knowledge/features/business-cabinet/ux-ui-flow.md

2026-07-29 | **The nav's glass was still visibly see-through after the same-day blur bump
(20px→32px, entry below), and raising the background opacity 60%→82% alone (also this entry,
first pass) only made it look flat and grey — because `backdrop-filter` was not rendering AT
ALL, confirmed by launching a real headless Chromium via Playwright and reading
`getComputedStyle`: it reported `none` despite the CSS declaring `blur(32px) saturate(160%)`.**
Root cause, found by bisecting against the compiled `.next` CSS output: Lightning CSS (Tailwind
v4's CSS processor) drops the UNPREFIXED `backdrop-filter` declaration when it is hand-paired
with an identical `-webkit-backdrop-filter` twin AND the value is a MULTI-FUNCTION filter
(`blur() saturate()` together) — a single-function value (`.neu-scrim`'s `blur(4px)`) was
never affected, and letting the autoprefixer generate `-webkit-` itself (one declaration, not
two) produces the correct output. Fixed in the three affected rules (`.neu-topbar`,
`.neu-bottom-nav`, `.neu-nav-link`) by removing every hand-written `-webkit-backdrop-filter`
that duplicates a multi-function `backdrop-filter` — verified both properties now survive in
the compiled CSS and the blur visibly renders (screenshotted via Playwright). The background
opacity raise (60%→82%) and the second inset rim (opposite the existing one, so the glass edge
reads on both sides) from the first pass stand unchanged. A "liquid glass" reference library
(real-time SVG/WebGL background distortion) was raised and declined for this pass — a different
technique, a new runtime dependency, and its own decision, not a same-file CSS tweak | src/design-system/neumorphism.css

2026-07-29 | **Owner UI review of the just-built 5-step registration wizard produced six more
fixes, same day, on top of the entry directly below (D29).** (1) The nav's glass got noticeably
more blurred — `.neu-topbar`/`.neu-bottom-nav` backdrop-filter 20px→32px
(design-system/neumorphism.css). (2) `catalogSetupMode: ASK_MANAGED_IMPORT` REVERSED from
disabled to a real, submitted choice — see D29 in the architecture decision log and
business-cabinet/locks.md's Retired Locks for the full reasoning; the two catalog-setup cards on
step 2 are now genuine selectable radios with equal-height layout (`items-stretch`, `justify-center`
on the shorter card) fixing the mismatched-height look flagged in review. (3) The register page's
subtitle was removed and the header-to-form gap set to 48px (`gap-12`), matching the Home hero.
(4) Step 3's "Only online" control and (6) step 5's agreement checkbox both moved off a small
`.neu-chip` onto a new shared `ToggleRow` component (`business-cabinet/ui/ToggleRow.tsx`) — a
full-width row with an icon and a REAL switch/checkbox indicator built on the neumorui
`.neu-switch-*`/`.neu-checkbox-box` primitives (design-system/neumorphism.css), present in the
CSS since D25 but unused until now. (5) Step 4's verification-source picker
(`VerificationSources.tsx`) was redesigned from a flat wrapped row of text chips into a
responsive icon-card grid (one lucide glyph per platform — 2GIS/Kaspi/Ozon/Wildberries get
generic map/shopping icons since lucide ships no brand marks), and the page-level intro
paragraph in `RegisterStepLinks.tsx` was deleted — it was showing the exact same sentence as
`VerificationSources`' own hint, immediately above it | src/design-system/neumorphism.css,
src/business-cabinet/{model.ts (catalogSetupMode on SellerOnboardingValues), ui/{BusinessRegisterPage,
RegisterStepScope,RegisterStepDelivery,RegisterStepReview,RegisterStepLinks,VerificationSources}.tsx,
ui/ToggleRow.tsx (new)}, shared/i18n/messages/{en,ru,kk}.json,
AI_Knowledge/{ARCHITECTURE_PATTERN_FRONTEND.md (D29), features/business-cabinet/{README,contracts,
ux-ui-flow,locks}.md}

2026-07-29 | **The seller registration wizard grew from 3 to 5 steps and gained a real branch
map picker — a 10-item owner UI pass on `/app/business/register`, reversing two pieces the
2026-07-28 design had explicitly excluded (D28).** (1) Verification links moved off step 1 onto
their own step 4, skipped entirely when the legal form doesn't need it (`stepIsSkippable`,
model.ts). (2) A "review & confirm" step 5 was added — a read-only recap plus a required
agreement checkbox, now the submit button's home. (3) Step 3 gained a "Only online — no physical
branch" checkbox (UI-only, forces `pickupAvailable: false`) and, when pickup is Yes, a
Leaflet/OpenStreetMap branch picker (`BranchMapModal.tsx`, `BranchMapCanvas.tsx`,
`BranchList.tsx`, D28) — reversed from "deliberately absent" because `CreateBranchRequest` (read
from the backend source, not inferred) requires `latitude`/`longitude`, making the map load-
bearing rather than decorative. Drafted branches are real: `useSellerOnboarding.submit` POSTs
each via a new `api.createBranch` right after `onboardSeller` resolves with `businessId`, before
the session refresh — one branch failing is toasted and does not block registration or undo the
business. (4) Step 2 now shows (but keeps disabled) an `ASK_MANAGED_IMPORT` catalog-setup option
card with illustrative ₸ pricing that varies by `businessScope` — `catalogSetupMode` still
always submits `MANUAL` (business-cabinet locks.md, unchanged rule, larger surface). (5) The
category field (step 1) now opens an instant dropdown on focus even with an empty query, since
the backend returns its full BUSINESS list for `q: ""`. (6) The city chip input dropped its
visible "Add" button — Enter or a trailing comma commits. (7) The Home hero's headline-to-search
gap grew from `gap-8` to `gap-12` (48px), unrelated to the wizard. **Two explicit lock/gate
questions were raised and answered by the owner before starting, not assumed:** the
`ASK_MANAGED_IMPORT` UI ships disabled rather than functional (roadmap #8 still gates the real
dialog); the map picker ships fully functional, submitting real branches, rather than staged
locally-only, once `CreateBranchRequest`'s required lat/lng was confirmed from source. Verified:
full `npm run build` (lint, boundary fixtures, typecheck, static generation, D6/D19 rendering
contract) green; `next start` smoke-tested on :3100 (no browser automation available this
session — HTTP-level checks only: 200s, no server errors, all three locales' new i18n keys
resolve with no `MISSING_MESSAGE`) | src/business-cabinet/*, src/search/ui/HomePage.tsx,
src/design-system/neumorphism.css (`.neu-map-frame`, `.neu-map-pin`), package.json
(leaflet, react-leaflet, @types/leaflet), shared/i18n/messages/{en,ru,kk}.json,
AI_Knowledge/{ARCHITECTURE_PATTERN_FRONTEND.md (D28), features/business-cabinet/{README,
contracts,ux-ui-flow,locks}.md}

2026-07-28 | **The `search` slice shipped (roadmap Phase 1 #2) — Home + Catalog Page, the first
slice with a genuine SSR data fetch, which surfaced a real gap in the e2e harness.** Built
strictly from the same-day reconciled `features/search/contracts.md` (the local backend at
`:2020` was unreachable during the build, so nothing here was live-verified against a running
server — flagged to the user before starting). Three decisions worth recording: **(1) State is
the URL, never a `store.ts`** — `CatalogSearchParams` (model.ts) is the Catalog Page's one param
vocabulary; the route file parses it and calls `search()` server-side (D7), sort/filter controls
push new params via a client hook. Nothing here outlives one render, so no zustand store exists
for this slice. **(2) Result cards are non-interactive** — the Product Card modal (D10) belongs
to `catalog` (slice #3, not built yet); rather than link to a page that doesn't exist, cards
render full data with no click target at all (the "a reachable control must DO something" lock
read literally: no control is safer than a dead one). **(3) The e2e harness gained a second
`webServer`** (`e2e/mock-backend.mjs`, wired in `playwright.config.ts`) — `page.route` only
intercepts requests the BROWSER makes, but the Catalog Page's `POST /api/v1/search` is called
SERVER-SIDE from inside the Next.js process per D7, which no earlier slice's e2e coverage had
ever exercised (every previous authenticated page was client-rendered behind a thin route). The
mock backend selects its response by the query TEXT (a "magic string" convention, e.g.
`roses-empty`, `roses-error`) rather than a mutable per-test fixture, because it is one long-lived
process shared by every parallel test worker. Also corrected: `platform-ui-design/SKILL.md`'s
empty-state guidance still said the empty catalog "offers to send a request" — stale since the
`requests` feature was removed the same day (previous entry below); fixed to match
`features/search/ux-ui-flow.md`'s actual empty state (suggestions/clear-filters/other-mode).
**Found, not fixed:** `guard.spec.ts`'s and `business-register.spec.ts`'s customer-bounce
assertions (`toHaveURL(/\/app$/)`) and two `auth.spec.ts` role-modal tests were already failing
against the unmodified codebase (confirmed by stashing this slice's changes and re-running) —
the first two are stale against D27's redirect-target change, the last two are a pre-existing
flake unrelated to search. Out of scope for this change; raised here so they are not mistaken
for a regression it caused | src/search/*, src/app/app/(main)/{page,catalog/page}.tsx,
next.config.ts (images.remotePatterns wildcard — business-uploaded logos have no enumerable
host), shared/i18n/messages/{en,ru,kk}.json (search namespace), e2e/{search.spec.ts,
mock-backend.mjs} (new), playwright.config.ts (webServer array),
AI_Knowledge/features/search/{README,ux-ui-flow}.md, .claude/skills/platform-ui-design/SKILL.md

2026-07-28 | **The `requests` slice was REMOVED from the product — the one V1 slice that dies rather than ships (owner decision).** It was roadmap #5 and delivered "fallback requests": when a search returned nothing, the query was dispatched **automatically** to businesses whose profile description loosely matched it — a shop described as selling clothing would receive "sneakers". Removed for a **logic gap, not a missing backend**: the behaviour collapses at scale, because once every loosely-matching company answers, the customer is handed exactly the noise ASK exists to remove. That is the anti-marketplace mission failing inside its own core flow, so no rebuild is planned and it does NOT move to Phase 2. Backend deleted the `request` domain (`CustomerRequest`/`RequestTarget`/`SupplierResponse`) on 2026-07-21, which is the same decision arriving from the other side. Docs moved to `features/_archived/requests/` per the removal procedure — **archived, never deleted**, with the full rationale on its README so the idea cannot be silently re-proposed. No code was removed: `src/requests/` never existed. **Two things deliberately NOT changed:** PRODUCT_VISION UF 3.1 item 1 ("Overview (should be 'Requests') … **these are all chats**") is a different feature and survives intact — that cabinet tab is composed from `@/chats` and always was; and `search`'s empty state, which lost its request CTA and now ends in the response's own `suggestions`/`ambiguity`, a cleared filter, a widened radius, or the other mode — never a CTA pointing at something that does not exist (project lock) | AI_Knowledge/{ROADMAP.md (slice table renumbered 5–11), features/README.md, features/_archived/requests/* (moved), features/search/{README,ux-ui-flow}.md}

2026-07-28 | **The search form gets a goods/services mode toggle, and the slice lock forbidding one was RETIRED — the lock's premise had been deleted out from under it.** `features/search/locks.md` carried `No product/service scope toggle in the UI`, justified as *"One unified endpoint — the AI determines intent from the query."* That endpoint no longer exists: `UnifiedSearchRequest` was replaced by `POST /api/v1/search`, whose `mode` is `@NotNull` and whose `SearchScope` enum admits only `ITEM` and `SERVICE`. There is no "everything" option and no AI-infers-intent path left to protect — the API **refuses** a request that does not choose. So something must choose, and the choice was given to the customer rather than hidden in a default, because defaulting in code makes half the catalogue unreachable with no control to discover it — the same silent dead end D26's lock exists to prevent. Surrounding extension was proven insufficient before reversing (the lock system's rule 2): a code default hides content, and firing ITEM+SERVICE in parallel and merging would put final ordering in the client, violating the still-live server-capability lock. Appended to **PRODUCT_VISION UF 2.1** with date + justification (CORE files change by append only) — it names a distinction the vision already draws (§1 "goods and/or services"; UF 3.1's separate Products and Services tabs) rather than inventing one. The lock is retired, not deleted, so the reversal stays legible. Gate **G1b** opened and closed the same day | AI_Knowledge/{PRODUCT_VISION.md (§3 UF 2.1 append), ROADMAP.md (G1b), features/search/{locks,README,ux-ui-flow,contracts}.md}

2026-07-28 | **`features/search/contracts.md` and `features/catalog/contracts.md` reconciled to backend `dev` `ee542d9` — both were stale in nearly every field name, and catalog's was stale in ways that would have produced a slice built entirely on endpoints that do not exist.** The backend rewrote search four times on 2026-07-26/27 ("Major Refactor V10–V13 Search") and restructured modules across 14 commits / 1106 files. Read from the **Java DTOs**, not the backend's prose docs — the same discipline the e2e-stub lock encodes, for the same reason. **Search:** `scope`→`mode` (now REQUIRED), `intent_match`→`relevance` (the old value is now regex-REJECTED), `filters`+`overrides`→a single `explicitFilters`, `diagnostics` and `contactActions` gone, results now arrive **sectioned** (EXACT/ALTERNATIVE) rather than flat, and `radiusMeters` (1–100 000) arrived — **the vision's 100 km filter is now buildable**, shrinking gate G1 from four parked controls to three. **Catalog:** products are `items`; `GET /api/v1/products{,/{id}}` and the entire `catalog/capabilities` endpoint **never existed on `dev`**; seller paths moved to `businesses/{businessId}/items` with parent IDs dropped from single-entity operations; branch association became optional, so the documented M2M "branch selection is part of every product form" model is void. Three backend defects raised rather than worked around (P9.4): `openingSummary` is declared on `SearchCardResponse` but never assigned in `toCard()` (always null — the same shape as `suggestRoleExpansion`); `badges[]` ships hardcoded English (`official channel`, `complete card`, `pickup`); and `GET /api/v1/legal/documents` + `/businesses/*/storefront` sit in the public allowlist with no controllers | AI_Knowledge/{features/search/contracts.md, features/catalog/contracts.md, ROADMAP.md (G1, cross-repo table)}

2026-07-28 | **Slice #3's `/app/product/:id` deferred, and the reason is that D10's SEO half was already moot.** There is **no public item read of any kind** on `dev` — the `permitAll` list admits only search, cities, categories, business-profile, drops and business-media; `/api/v1/businesses/{id}/items` is authenticated, business-scoped, and a *list*, so using it as a detail read would fetch a whole catalogue to render one card. Decision: **ship D10's modal half** — the vision's actual requirement (UF 2.1 step 3) — rendered from the `SearchCardResponse` payload the catalog list already holds, and defer the deep-link page until the backend exposes a public item read (raised in the cross-repo table). The cost is smaller than it first looks: D23 put every `/app/*` surface behind the client auth gate, so those pages have not been crawlable since 2026-07-21 and the deep link's SEO value was already zero — what is genuinely lost is the **direct/shared link**, nothing more. The binding constraint for the card's design: it may show only fields `SearchCardResponse` carries, and no others (P9.4) | AI_Knowledge/{features/catalog/contracts.md, ROADMAP.md (slice #3, cross-repo table)}

2026-07-28 | **Gate G3 ("Proceed to Purchase") RE-OPENED — its standing answer was deleted.** The gate has always parked one button, with the backend's `contact` module (the `contactActionId` privacy pattern) as the presumed resolution. That module was **deleted on 2026-07-21**, and `contactActions` is gone from `SearchCardResponse`; no contact-action concept survives anywhere on the wire. What remains on the card is `businessProfile.{number, email, websiteUrl, instagramUrl, telegramUrl}` — the business's **public** channels, not a privacy-preserving handle. Recorded explicitly in the gate that routing the button there is a **product decision** (it exposes real contact details, which the deleted module existed precisely to avoid), not a fallback an agent may quietly pick. The gate still parks only the click handler; the whole Product Card, its fields, and the chat button ship regardless | AI_Knowledge/ROADMAP.md (G3, cross-repo table)

2026-07-28 | **The sign-up consent checkbox was found to go nowhere, and the backend shipped its home the same week.** `RegisterForm` collects `acceptedUserAgreement` and `hooks.ts` sends it, but the backend removed that field from `CustomerRegisterRequest` and Spring Boot ignores unknown JSON properties by default — so registration returns **201 and the consent is silently discarded**, invisibly, on a control that gates the form and links to Terms/Privacy. Previously logged as an open question for backend/product; it is now a **client task**, because `dev` added a `legal` module: `POST /api/v1/legal/registration-acceptances` (Bearer) taking `documentCodes` from a 7-value enum plus `countryCode`/`locale`. Queued as an auth follow-up with one explicit constraint — **send only `USER_TERMS` + `PRIVACY_POLICY`, the two documents the form visibly links to.** `GET /api/v1/legal/documents` is allowlisted but has no controller, so the active document set cannot be discovered; submitting codes for text the user was never shown would be a worse defect than the one being fixed (P9.4) | AI_Knowledge/ROADMAP.md (auth follow-up, cross-repo table)

2026-07-28 | **`RequireDashboardAccess` now bounces a customer-only session to `/app/business/register`, not `/app` (D27, amends D23/D26's redirect target).** The nav's Dashboard link and the route guard already shared one predicate (`canAccessDashboard`, P6.2) — the link appears automatically once `toAuthUser` reads a `business`/`staff` role off the session, so that half of "give sellers a Dashboard link automatically" was already correct. The gap was the OTHER direction: a customer-only session hitting `/app/business` (typed, bookmarked, or reached before onboarding) was bounced to Home, past the `business/register/` door D26 built as the guard's own sibling route specifically so a non-seller could reach it. One-line fix in `RequireDashboardAccess.tsx` — `router.replace("/app")` → `router.replace("/app/business/register")` — no new route, no new predicate, no change to the nav. Closes the loop end to end: not-yet-seller hits the Dashboard route → lands on registration → submits → `useRefreshSession()` (D26) updates `canAccessDashboard` → the register page's own redirect (`BusinessRegisterPage.tsx`, unchanged) and the guard both now agree the account may enter → the cabinet renders, with the Dashboard link already visible in the nav | src/auth/ui/RequireDashboardAccess.tsx, AI_Knowledge/{ARCHITECTURE_PATTERN_FRONTEND.md (§2, §11 D27), Locks.md, features/auth/{locks,README,ux-ui-flow}.md}

2026-07-28 | **`NavigationMenu` redesigned twice in one session (owner-driven, iterative review) — first the desktop chrome, then a fully separate mobile layout.** (1) The destinations left `.neu-tab-list` (the shared single-groove segmented control still used by theme/language toggles and filter chips) — navigating between pages now reads as picking a place, not choosing a setting. Each destination is its own `.neu-nav-link` glass pill (translucent `color-mix` + `backdrop-filter` + a raised neumorphic shadow, i.e. liquid-glass + neumorphism combined, per owner request), and ONE `.neu-nav-indicator` slides beneath whichever link is current — a GSAP FLIP-style tween (shared/motion.ts, D14) using `x`/`scaleX` transform ONLY (never width/left, the motion lock): the indicator's box is snapped to the new link's rect instantly, then the visual delta from the old rect is played back and settles at `scaleX: 1`, so there is never a left-over corner distortion at rest. A second `<ul>`/ref-set swap (desktop top bar ⇄ mobile bottom bar) is guarded against replaying as a slide-across-the-screen glitch by tracking the mounted `<ul>` node itself, not just the active href. (2) **Mobile then dropped the top bar ENTIRELY** (owner decision, after reviewing the neumorui `bottom-nav` reference, `https://neumorui.vercel.app/docs/bottom-nav`) rather than keeping a shrunken responsive header — a phone's thumb reaches the bottom of the screen, not the top-right (§7). The same destinations render as a fixed `.neu-bottom-nav` bar, stacked icon-over-label, PLUS the account avatar as a 4th tab (`t("pages.profile")` label, reusing the existing key rather than adding one) — settings/logout have nowhere else to live once the top bar is gone, so its `DropdownMenuContent` opens `side="top"`. `.neu-skin:has(.neu-bottom-nav)` reserves matching bottom padding on `<sm` so page content never sits underneath the fixed bar. Bottom-nav tabs are FLAT at rest (no glass panel, no shadow — only the travelling indicator and `:focus-visible` ever paint a surface), matching the reference pattern exactly, after a review round caught every tab carrying its own resting shadow (looked like four stacked cards instead of one strip). `renderLink`/`renderAccountTrigger` factor the row (desktop) vs. stacked (mobile) markup from one source so the two layouts can never drift apart. Verified with a throwaway Playwright script driving the real dev server (session stubbed like `e2e/navigation.spec.ts` already does) — desktop/mobile × light/dark, the account menu opening from both trigger shapes, and a mid-tween frame of the indicator — rather than trusting the diff; `tsc`/`eslint`/`prettier` all clean | src/app/_components/NavigationMenu.tsx, src/design-system/neumorphism.css

2026-07-28 | **Seller registration converted from one long form to a 3-step wizard (owner request), after the retired React Router frontend's `SellerOnboardingPage` was found on the backend repo's `dev` git branch and consulted as prior art.** That page (not deployed, never ported to this Next.js codebase) grouped the same fields into 4 steps ending in a branch-drafting/map-picker pickup modal and a legal-consent confirmation screen; both were deliberately NOT ported — the map-picker modal duplicates the cabinet's own Branches tab (already this doc's rule), and there is no `acceptLegalDocuments` endpoint or consent-persistence mechanism verified to exist in the CURRENT backend (raised separately, not invented here). Scoped down to **3 steps**: (1) identity + legal proof — grouped together because the backend's own `@AssertTrue` rules on `SellerOnboardingRequest` key both off `legalForm`, so the step boundary matches the validation boundary; (2) what you sell (`businessScope`, no required answer — can never block advancing); (3) delivery + pickup. `useSellerOnboarding` gained `step`/`goNext`/`goBack`. **Caught driving it in a real browser (production build, port 3100) rather than trusting the diff:** the first version of `goNext` validated the WHOLE form on every advance, which put step 3's errors ("choose where you deliver") into state — and onto the screen — the instant step 2 was left, before the person had touched step 3 at all. Replaced with `validateOnboardingStep` (model.ts), which validates only the current step; each field's own change handler already clears its own error as soon as it's fixed, so `goNext` only ever needs to REVEAL a problem, never resolve one on a step not yet reached. A second bug surfaced fixing the first: `validateOnboardingStep`'s original body used object destructuring (`const { businessName, ... } = full; return { businessName, ... }`), which puts `businessName: undefined` on the returned object even when step 1 is clean — and `hasOnboardingErrors` counts `Object.keys`, so an all-`undefined` object still read as "has errors", silently blocking `goNext` from ever advancing past step 1. Fixed by only assigning a key when the full validator actually set it. The single shared submit button reads "Next" until the last step, then "Create business", and Enter-to-submit inside a text field now doubles as Enter-to-advance for free. `BusinessRegisterPage.tsx` split into `RegisterStepIdentity`/`RegisterStepScope`/`RegisterStepDelivery` purely to hold the cabinet's 400-line lock (P1.1) — the steps share one values object, not three separate concerns. Sections separated by `.neu-rule` (the existing depth-based divider component already used in `OAuthOptions`/`dropdown-menu`/`select`), not an invented divider style — see the same-day `.neu-rule` visibility fix below. e2e spec rewritten to drive the full step sequence per test, plus two new tests (`goBack` preserves typed values; step 1 refuses to advance without a legal form) | src/business-cabinet/{model,hooks}.ts, src/business-cabinet/ui/{BusinessRegisterPage,RegisterStepIdentity (new),RegisterStepScope (new),RegisterStepDelivery (new),DeliveryCitiesField}.tsx, src/shared/i18n/messages/{ru,kk,en}.json, e2e/business-register.spec.ts, AI_Knowledge/features/business-cabinet/ux-ui-flow.md

2026-07-28 | **`.neu-rule` (the shared divider primitive) was dark-on-dark everywhere it was used, not just on the new register wizard — found while reviewing that wizard, fixed at the source.** Every consumer (`OAuthOptions`' "or", the account menu's `DropdownMenuSeparator`, `SelectSeparator`, the register wizard's own section dividers) left the class fill-less, relying on `--neu-inset-sm`'s box-shadow alone; that shadow was designed for buttons, not a 2px line. Three rounds, each verified in a real browser rather than assumed from the diff: (1) added `background: var(--surface-sunken)` — visible on the register page in isolation, but measured at only ~1.08:1 against `--surface` (an 8/255 per-channel gap), so it still read as a dark smudge once the owner compared it against the rest of the page. (2) Swapped to `background: var(--border)` (~1.31:1) and moved the fix from a per-component override into `.neu-rule` itself so every consumer — not just the one page — picks it up; caught because the owner's next screenshot was the account menu's separator, not the register page. (3) **The actual root cause:** even with a correctly-applied, correctly-measured fill colour, the owner reported it "almost invisible" again — isolating `.neu-rule` in a bare HTML repro (no app) showed `--neu-inset-sm`'s 8–10px blur is 4–5x the element's own 2px height, so the inset shadow paints as one solid smear that drowns any fill colour underneath regardless of which token is chosen; the first two corrections were real but capped by a shadow sized for the wrong kind of element. Added `--neu-inset-hairline` (1–2px blur, proportional to a 2px box) to both themes plus the no-JS dark fallback (`design-system/neumorphism.css`'s own rule: change the two dark blocks together), and pointed `.neu-rule` at it instead of `--neu-inset-sm`. Re-verified all three known consumers in a real browser, dark AND light, by re-inspecting rendered screenshots rather than computed styles alone — computed styles had looked "correct" at every prior step, since the bug was in how the blur COMPOSITES with a thin box, not in which value was declared | src/design-system/neumorphism.css

2026-07-28 | **Seller registration was building a request the real backend would 400 on — `deliveryCoverage`/`pickupAvailable` (both `@NotNull`) and conditionally `deliveryCities` were never asked for.** Found by reading `kz.ask.business.onboarding.api.dto.SellerOnboardingRequest` directly on `dev` (`ee542d9`) instead of trusting `contracts.md`, which the D26 change had itself written from the DTO but incompletely. Added a `deliveryCoverage` segmented control (`NO_DELIVERY`/`SELECTED_CITIES`/`KAZAKHSTAN`/`WORLDWIDE`), a new free-text `DeliveryCitiesField` (chip list, Enter-to-add, required only when `SELECTED_CITIES` — mirrors the backend's `isDeliveryCoverageValid`) and a Yes/No `pickupAvailable` control to `BusinessRegisterPage`, all validated client-side against the same rules the backend asserts. **No vision entry exists for delivery/pickup (P9.1)** — these are backend-required fields with no product description, built anyway because the endpoint is otherwise unusable; raised as a doc gap for product/backend to close, not invented UI in the sense the lock means to prevent (the screen itself — seller registration — is already in the vision; these are missing fields on an existing screen, discovered only by reading the DTO). Also corrected a stale doc: `contracts.md` still named the onboarding error code `SELLER_ONBOARDING_INVALID` in one place after the D26 change had renamed it everywhere else — the real code is `BUSINESS_ONBOARDING_INVALID`, which `hooks.ts` already had right | src/business-cabinet/{model,hooks}.ts, src/business-cabinet/ui/{BusinessRegisterPage,DeliveryCitiesField (new)}.tsx, src/shared/i18n/messages/{ru,kk,en}.json, e2e/business-register.spec.ts, AI_Knowledge/features/business-cabinet/{contracts.md,ux-ui-flow.md}

2026-07-28 | **Verify-code auto-submit rejected a code the screen already showed as complete.** `CodeInput`'s `onComplete` fires synchronously in the same tick as the `setCode()` call that completed the sixth digit, before React commits that state update; `useVerifyStep.submit()` read `code` from its own closure, so it validated the PREVIOUS (5-digit) value against `CODE_RE` and threw "Enter the 6-digit code" even though all six cells were visibly filled (e.g. six `1`s). `submit` now takes an optional `codeOverride`, and `VerifyCodeForm`'s `onComplete` passes through the value `CodeInput` already computed instead of waiting on the closure to catch up; manual submit (Enter / the Confirm button) is unaffected since by then the state has committed | src/auth/hooks.ts, src/auth/ui/VerifyCodeForm.tsx

2026-07-27 | **The six `(main)` placeholder routes (`/app`, `/catalog`, `/chats`, `/product/:id`, `/profile`, `/business`) ignored the `ask.locale` cookie — fixed to match the pattern auth routes already used.** Each called `getTranslations("app")` with no locale argument, which always resolves to `defaultLocale` (`shared/i18n/request.ts` never reads the cookie itself), so a user who switched to `en` or `ru` still saw these placeholders in `kk`. Fixed by resolving `localeFromCookies() ?? defaultLocale` first, same as the auth routes' `generateMetadata` and `app/layout.tsx` already do (D19) — no new mechanism, just applying the existing one where it was missed. **Separately, `LocaleProvider`'s mount effect stopped writing to localStorage**: on first mount `stored` is still `undefined` (the hydration-safe server snapshot must render before the real client value is read), so `locale` briefly equals `initialLocale` (the cookie) — writing that back to localStorage on every mount could stomp an explicit `setLocale()` preference with a stale cookie value on the next load, which is what produced the nav and page body rendering two different locales at once. localStorage is now written only by the explicit `setLocale()` call | src/app/app/(main)/{page,catalog/page,chats/page,profile/page,business/(cabinet)/page,product/[id]/page}.tsx, src/shared/i18n/LocaleProvider.tsx

2026-07-27 | **THE CLIENT NOW TARGETS THE BACKEND'S `dev` BRANCH (owner decision) — and the road to that decision is the lesson.** Reported as "no modal appears after registration". Two independent causes were found by reading the Java DTOs; **one diagnosis was right, the other was branch-confusion, and the difference only showed when the RUNNING server was finally probed.** **(1) The id field — a mistake worth recording.** `../Ask_Backend` is checked out on `dev`, where the DTO is `VerificationResponse.verificationId`. Reading only that, `authChallengeId` was "corrected" to `verificationId` — and sign-up broke instantly, because `localhost:2020` still runs a `master` build that answers `400 VALIDATION_ERROR` naming `authChallengeId`. The claim "every verification 400'd against the live backend" was **exactly backwards**: it worked before the change and broke after it. Established by curl, not by reading: register+verify round-trips succeed on `master` with `auth_challenge_id`, and fail with `verification_id`. The owner then chose to target `dev` regardless, since that is what deploys — so `verificationId` stands, **with the consequence written down** rather than discovered later: verify and all of `/app/business/register` will fail against the current :2020 build until it redeploys from `dev` (the seller onboarding endpoint 404s there entirely, and `/categories` returns a tree instead of the flat suggestion list). **The durable rule: the checkout says where we are going, the running server says what works now, and neither is sole authority — confirm against a real response and record which branch it came from.** **(2) `suggestRoleExpansion` is declared and never assigned** — this one was NOT branch-dependent and holds on both (`git grep` finds nothing on master; declared-but-never-built on dev; the live verify response confirms `suggest_role_expansion: null`), so the fix built on it stands — verified across the whole backend source, not one `.suggestRoleExpansion(...)` builder call — so the documented modal trigger was permanently null and the modal PRODUCT_VISION UF 1 step 3 makes unconditional after sign-up could not open on any account. The modal is now armed by **`purpose === "REGISTER"`** on the challenge, which is REAL backend data from the same flow (`VerificationPurpose.REGISTER`), OR'd with `suggestRoleExpansion` so nothing changes the day it starts arriving. The auth slice lock was AMENDED rather than broken: its intent was "a real backend signal, not a fabricated intent fork", and that holds — only the field carrying the signal changed, and no surrounding extension existed because no other populated field distinguishes a fresh sign-up. A log-in 2FA challenge deliberately carries no purpose (`useLoginFlow` builds that Challenge by hand), so signing in never re-opens an answered choice. **Google OAuth is NOT fixed and is not guessed at:** `/oauth/callback` has no first-signup signal on the wire at all, so a Google-first account still gets no modal — raised with backend (P9.4). **The reason none of this was caught is now its own lock:** the e2e stubs answered with `auth_challenge_id` and a hand-written `suggest_role_expansion: true`, i.e. the client's own assumptions, so the suite proved only that the client agreed with itself. Every stub key was re-read from the Java DTOs, and `suggest_role_expansion` was REMOVED from the fixture so the tests now exercise the real trigger. Two `*_old` archive files took the mechanical field rename (they import the live `Challenge` type, so a shared-type rename necessarily reaches them). Verified: `eslint src`, `tsc --noEmit`, `prettier --check` all clean | src/auth/{model.ts,hooks.ts}, src/auth/ui/{VerifyCodeForm,RegisterForm,LoginForm}.tsx, src/auth/ui/{RegisterForm,LoginForm}_old.tsx, e2e/auth.spec.ts, AI_Knowledge/features/auth/{contracts.md,locks.md,ux-ui-flow.md}, AI_Knowledge/Locks.md

2026-07-27 | **Seller registration built at `/app/business/register` (D26) — closing a reachable control that silently did nothing.** Owner directive, choosing "build the real page" over an interim notice. The role modal's "set up your business" routed to `/app/business`, where `RequireDashboardAccess` bounced the customer-only session that had just chosen it; the fork looked live and swallowed the click, invisible in review because the modal's target and the guard were each individually correct. It now routes to registration — PRODUCT_VISION UF 3.1, "the seller is redirected to the business registration page". **`business-cabinet` gets its first code ahead of the cabinet it is named for**, justified because this is the door the cabinet's guard is guarding, not a speculative primitive (P8.1). **The access fix is structural, not conditional:** `RequireDashboardAccess` moved from `business/layout.tsx` (which covered the whole prefix) into a new `business/(cabinet)/` group, with `business/register/` as the one sibling outside it — placement IS status, the same grammar as `(main)` being the auth gate line, so a new cabinet tab is protected by being put in the group and a per-URL allowlist was never needed. **The second half is the session refresh:** a 201 from `POST /api/v1/business/onboarding` promotes the account to BUSINESS_OWNER server-side, so without re-reading `GET /auth/session` the client still thinks it is a customer and the guard bounces the brand-new seller out of the cabinet they just created — `useRefreshSession()` added to `@/auth` (the session is auth's to own, R6; a slice patching the store would assert a role the backend never confirmed, P9.4). **The documented endpoint did not exist:** `contracts.md` had `POST /api/v1/seller/onboarding` with `catalogScope: PRODUCTS|SERVICES|BOTH` and `SELLER_ONBOARDING_INVALID`, all written from a changelog entry rather than the source and all renamed by the backend's 2026-07-21/22 cleanup; corrected against `kz.ask.business.onboarding.api.*`. **Two values are PINNED rather than offered, and that is the point:** `catalogSetupMode: MANUAL` (the other value opens a managed-import dialog that does not exist until roadmap #8) and `countryCode: KZ` (the legal forms on offer are Kazakhstan's; a one-option picker is a dead control, and a second market is gate G4). Offering either would have rebuilt the exact dead end this page removes — now its own lock. Category is a combobox where **free text is a first-class outcome**, not a fallback: the backend accepts `categoryName` and creates the USER category itself. Verification sources are progressive (pick platforms, then fill only those) per the backend's own UX contract. New e2e spec covers the customer reaching the page, the cabinet still bouncing that same customer, the role flipping on submit, the at-least-one-valid-link rule, and an existing seller being sent to the cabinet instead of registering twice | src/business-cabinet/{index,api,model,hooks}.ts + ui/{BusinessRegisterPage,CategoryField,OptionGroup,VerificationSources,Field}.tsx (new), src/app/app/(main)/business/{(cabinet)/{layout,page}.tsx,register/page.tsx}, src/auth/{hooks.ts,index.ts}, src/auth/ui/RoleSelectionModal.tsx, src/design-system/neumorphism.css, src/shared/i18n/messages/{ru,kk,en}.json, e2e/business-register.spec.ts (new), AI_Knowledge/{ARCHITECTURE_PATTERN_FRONTEND.md §2+D26,Locks.md,features/business-cabinet/*}

2026-07-27 | **Password strength meter on sign-up (owner request — neumorui's PasswordInput).** `auth/ui/PasswordInput` gained an opt-in `showStrength`, set on exactly ONE field in the product: the new password at sign-up. Not the confirmation (a transcription of the field above it) and not login (nobody can act on a verdict about a password they already have). Scored by `passwordStrength()` in `auth/model.ts` — pure and DOM-free, five independent checks (≥8 · ≥12 · mixed case · digit · symbol) rather than an entropy estimate, because this has to be understood at a glance while typing; length counts twice deliberately, since it is the property that actually dominates guessing cost. **It is a writing aid, never a gate** — the only rule that can block a sign-up is the backend's 8–128 bound, and a meter refusing a password the server would accept would be inventing policy the data authority never stated (P9.4). Rendered as new `.neu-meter-*` classes (a thinner groove than `.neu-progress-*`, no entry animation — a 1.1s grow would still be catching up two keystrokes later). **Not a traffic-light violation:** that lock forbids scoring a BUSINESS red/amber/green, because a score is a rating by another name; this scores the viewer's own draft password in a form they are filling in, no third party judged — and colour is never the only channel, since the fill's width and the word beside it carry the same verdict. The three semantic colours were measured as TEXT on the surface for the first time (they had only ever been measured as fills carrying a label) and the numbers written into the file's verification table per the measurement lock: `warning` light is the tight one at 4.88:1, with no headroom to spend | src/auth/model.ts, src/auth/ui/{PasswordInput,RegisterForm}.tsx, src/design-system/neumorphism.css, src/shared/i18n/messages/{ru,kk,en}.json, AI_Knowledge/features/auth/ux-ui-flow.md

2026-07-27 | **D25 follow-up: portaled overlays were rendering UNSTYLED; fixed by portaling INSIDE the skin, and one racy e2e test corrected.** Driving the role modal in a browser (not reading the diff) showed its cards at `box-shadow: none` and its Continue button at `rgba(0,0,0,0)` — the exact failure D25's own portal lock predicted, missed in the very component that change rewrote. Radix portals Dialog/DropdownMenu/Select into `<body>`, outside `neu-skin`, where colour utilities take the MARKETING palette **and all 84 `.neu-*` rules match nothing**. The original fix (re-declaring a few variables on the portal node via `data-neu-portal`) was abandoned rather than extended: once the component RULES must work out there too, "a handful of variables" is the entire palette duplicated across light, dark and the no-JS block — three copies to keep in step forever. Instead `shared/ui/skin-portal.ts` exposes `useSkinPortalContainer()`, which finds `#ask-skin-root` (the wrapper) so every overlay portals back INSIDE the scope and inherits the whole skin with nothing duplicated; the `[data-neu-portal]` blocks and attributes are gone, the panels now simply wear `.neu-card`. Putting the palette on `:root` was rejected again — it would leak the skin onto the landing, which is what the scope exists to prevent. New lock: the wrapper must never take a transform/filter/perspective, since any of the three would make it a containing block and re-anchor every fixed overlay. **Separately**, `auth.spec.ts`'s role-modal test was corrected, NOT to make it pass: it stubbed a customer-only session, then asserted the modal routes to `/app/business` — a URL `RequireDashboardAccess` is designed to bounce. Measured on both this commit and its parent, the URL held for 8–25ms, so the assertion was a coin flip on whether a ~350ms poll landed inside it; CI won that flip for weeks and lost it on this push. The stub now restores a SELLER session, removing an unrelated guard from a test about the role modal (guard behaviour is covered on its own in `guard.spec.ts`). The first attempt at this looked right and did nothing — it set `role: "BUSINESS_OWNER"` but omitted `business`, and `toAuthUser` DEGRADES any business role to `customer` when the backend sent no business context rather than fabricating one (P9.4), so the role silently stayed customer and the bounce still fired. Caught by instrumenting the run rather than re-reading it: the Dashboard link was absent from the nav at click time, which is the live proof of what the session actually thinks. With the context added the URL now HOLDS instead of being caught mid-flight — 8/8 with no redirect at all, 12/12 on the test, and the full suite twice with retries DISABLED. Verified: build green, e2e 62/62 at CI's worker count, `tsc`/`eslint`/`prettier --check .` clean, and the modal re-driven in light and dark | src/shared/ui/skin-portal.ts (new), src/shared/ui/{dialog,dropdown-menu,select}.tsx, src/app/app/layout.tsx, src/design-system/neumorphism.css, e2e/auth.spec.ts, AI_Knowledge/{Locks.md,ARCHITECTURE_PATTERN_FRONTEND.md §11 (D25)}, .claude/skills/platform-ui-design/SKILL.md

2026-07-27 | **The `/demo` neumorphic skin PROMOTED to the product (D25) — every surface except the marketing landing; the retired skin kept verbatim as `*_old`.** Owner directive, in two parts: adopt this style everywhere but the landing (which gets its own direction later), and *"do not remove old styles, leave them but rename everything as `..._old`"*. Upstream is **neumorui** (`rukonpro/neumorui`, https://neumorui.vercel.app), via the D24 lab port. **The mechanism, which is the real content of this change:** `tokens.css` → `tokens_old.css`, still imported and still the ONE `@theme inline` Tailwind theme — because `@theme inline` inlines literal values for radii/shadow geometry but resolves colours through `var(--token)`, redefining the colour primitives inside a `.neu-skin` scope re-tints every colour utility in the platform while the landing keeps the old palette. New `design-system/neumorphism.css` holds those overrides plus the depth language as `.neu-*` classes in `@layer components` (below Tailwind's utilities layer, so `className="neu-btn w-full"` works with no `!important` — a fact worth knowing, since the D24 port had the opposite problem and lost silently to specificity). `.neu-skin` is applied in exactly ONE place, the platform layout. **Three lab values could not ship and were changed — all darkenings, all measured, all recorded in the file's verification table:** `#ff6a2c` holds white at 2.86:1 (demoted to `--neu-accent-vivid` for non-text fills; text-bearing surfaces take the gradient's dark stop `#c1440e`, 5.14:1 — the same call the owner already made for chat bubbles in the lab); `#7a6c58` secondary text at 4.39:1 and `#bcae98` at 1.86:1 (re-derived / demoted to decorative); the success/warning/danger inks were tint-on-tint and failed as text. **The portal hazard, found while porting and now a lock:** Radix renders Dialog/DropdownMenu/Select content into `<body>`, OUTSIDE the scope — where a colour utility silently resolves to the MARKETING palette and a skin-scoped `.neu-*` class silently does nothing. Both are invisible in review and only show in a browser; fixed with a `data-neu-portal` opt-in that re-declares the variables it needs, plus dual-scoping for `.neu-rule`/`.neu-menu-item`. Sonner is the inverse: it renders IN PLACE, so its host moved inside the wrapper, and its shape is stated UNLAYERED because sonner's own stylesheet is unlayered and beats any layered rule regardless of specificity. **Verification-page OTP (owner request, mid-session):** the single 6-char field became six cells (neumorui's OTPInput) — `auth/ui/CodeInput.tsx`, slice-private per D8, with paste-anywhere, SMS autofill, backspace-steps-back, arrow/Home/End, per-cell `aria-label` ("Digit 3 of 6", new `auth.verify.digitAria` key in ru/kk/en), and submit-on-complete; `code` remains one string owned by `useVerifyStep`, so the flow and the request are unchanged. **Two design locks retired, not deleted** (Locks.md gained a "Retired Locks" section): *quiet chrome* (superseded — depth IS the language now) and *saturation-is-action* (NARROWED: still the only fill on anything carrying text, and the offer chip is still a tint — `TINT IS INFORMATION` survives intact). The /demo exemption lock is retired as spent: it existed to make exactly this promotion deliberate, and was honoured — nothing moved by file copy, the values were re-derived and re-measured, and /demo keeps its own independent `.neu-lab`. **The `*_old` archive is a restorable set, not loose backups:** each `_old` file imports its `_old` siblings, so the previous skin restores as a coherent whole; five archives whose live sibling never changed (spinner, GuardFallback, the two Pages, OAuthCallbackPage) are kept precisely because the set needs them to compose. Marketing is untouched and provably isolated — it imports nothing from `shared/ui`. Verified: `tsc --noEmit` clean, `eslint src` clean, `prettier --check` clean. NOT run (needs an explicit "run"/"build"): `next build` and the e2e suite — the skin, the portal opt-ins and the OTP field await that visual pass | src/design-system/{tokens_old.css (renamed),neumorphism.css (new)}, src/app/globals.css, src/app/app/layout.tsx, src/app/_components/NavigationMenu.tsx, src/shared/ui/*.tsx (12 reskinned + 13 `_old`), src/auth/ui/{AuthShell,Field,LoginForm,RegisterForm,VerifyCodeForm,PasswordInput,OAuthOptions,RoleSelectionModal}.tsx, src/auth/ui/CodeInput.tsx (new), src/auth/ui/*_old.tsx (12), src/shared/i18n/messages/{ru,kk,en}.json, AI_Knowledge/{Locks.md,ARCHITECTURE_PATTERN_FRONTEND.md §11 (D25)}

2026-07-27 | **`/demo` rebuilt as a faithful, orange-recoloured port of neumorui (D24), replacing the plain baseline from earlier the same day.** Owner directive: take every style and component from `rukonpro/neumorui` (a 124-component neumorphic React kit), recolour the accent/info hue from the library's default violet to orange (neutral bg/shadow hue warmed to match; success/danger/warning kept as measured), and initially also borrow NichNeumor's (`Nichtigott/NichNeumor` theme.css) tightly-packed "glued" button-group placement — that second source was dropped mid-session on owner instruction ("I can add it later") once the first pass proved too shallow. The first pass approximated neumorui from its docs/marketing copy (invented shadow values, a generic transition, no per-component sizing) and shipped with a real defect: the Switch's old Tailwind sizing (`h-6 w-11`, `translate-x-5`) fought the new CSS's actual track size, overlapping the thumb into the label text. Corrected by pulling the LIBRARY'S OWN SOURCE (`packages/core/src/tokens/{neu,animations,index}.css` + each component's inline style objects — Button, Switch, Checkbox, RadioGroup, Slider, Progress, Tooltip, Toast, Modal, Tabs, ScrollArea, Skeleton, EmptyState, Card, Avatar, Chip, Input) via the GitHub API/raw content, and porting the exact numbers: per-component radii (Button 12–14px, Card/Modal 24px, Switch a 999px 54×30 track with a 24px thumb, Checkbox 9px, EmptyState 18–20px…), the exact dual-shadow formulas (`--neu-shadow-raised/-sm/-lg`, `-inset/-sm`), the exact easing curves (`cubic-bezier(0.34,1.4,0.64,1)` general, the bouncier `cubic-bezier(0.34,1.56,0.64,1)` for switch/checkbox/radio/slider-thumb/modal), and the real keyframes (`neu-radio-pop`, `neu-check-pop`, `neu-slide-down`, `neu-slide-in-right`, `neu-scale-in`, `neu-overlay-fade-in`, `neu-grow-bar`, a `background-position`-driven shimmer) — plus the dynamic states the first pass lacked entirely: buttons lift `translateY(-3px)` + grow their shadow on hover and squash to `scale(0.97)` + invert to inset on press; cards lift `-5px`; the radio dot and checkbox tick pop in with an overshoot; the active tab/segmented-control trigger presses in (`box-shadow: inset`) and its label takes the accent colour, verbatim from neumorui's own `[data-state="active"].neu-tab-active` rule. Two structural bugs surfaced and were fixed while porting: (1) several one-off Tailwind utility overrides (`p-4`, `p-0`, `h-8 w-8` on `.neu-btn`-classed elements) were silently dead because a two-class scoped selector (`.neu-lab .neu-btn`) always outranks a single Tailwind utility class at equal source order — fixed with dedicated compound-selector modifiers (`.neu-btn.neu-modal-close`, `.neu-btn.neu-modal-option`) instead of fighting specificity from JSX; (2) the Modal's entrance keyframe assumes `position:fixed; left/top:50%` with the translate baked into the keyframe itself (matching neumorui's own centering technique) — the content panel's JSX was aligned to that instead of a flex/grid-centered overlay, so the animation's `translate(-50%,-50%) scale()` doesn't fight a different centering mechanism. `design-system/tokens.css` (D3) was not touched at any point | src/app/demo/neumor.css (new, replaces the plain baseline), src/app/demo/DemoLab.tsx, src/app/demo/LabComponents.tsx, src/app/demo/page.tsx
2026-07-27 | **Both `/demo` candidate skins (glass, neu) retired; the lab reset to a plain, skin-agnostic baseline (owner directive).** Owner judged the two 2026-07-22/23 candidates and is porting in a single new style from two reference repos instead of continuing to compare skins. Removed outright: `glass.css`, `neu.css`, the `Glass / Quiet / Neu` switch and its `useState`/`data-skin` wiring in `DemoLab.tsx`, the ambient `.glass-field` backdrop, and every `glass-panel`/`glass-well`/`glass-rail`/`glass-plate`/`glass-scrim` chrome hook in `DemoLab.tsx` and `LabComponents.tsx` — components now carry only their structural Tailwind + design-system token classes, identical to what the retired "Quiet" control rendered. `page.tsx`'s locks-off rationale updated to describe the retirement rather than the two-skin comparison it no longer runs. D24 itself is untouched: `/demo` remains the permanent design lab, still exempt from the vision lock only, still noindex, still outside `(marketing)`/`/app/*`. Nothing entered `tokens.css` at any point in the glass/neu experiment, so nothing needed to be unwound there | src/app/demo/DemoLab.tsx, src/app/demo/LabComponents.tsx, src/app/demo/page.tsx, src/app/demo/glass.css (deleted), src/app/demo/neu.css (deleted)
2026-07-23 | **Neumorphism added to `/demo` as a SECOND candidate skin (D24) beside glass, and the flat baseline's switch label renamed `Flat` → `Quiet`.** Owner asked to see the neumorui (`rukonpro/neumorui`) "clay" look on the lab. Same discipline as glass, and the same refusal of the obvious route — the package ships inline styles and its own `#f0f4ff`/`#6c7ef8` palette, radii and shadows, i.e. a **live design source** D12 forbids; rebuilt instead as ~1 scoped stylesheet (`neu.css`, `[data-skin="neu"]`) on ASK's tokens. **The ONE idea: depth is a shadow, not a border** — a neumorphic element is the SAME colour as its surface and reads as raised/recessed from a dual cast (light top-left, dark bottom-right). Two things keep it inside the locks: (1) *the fill stays OPAQUE*, so unlike glass every tokens.css text ratio holds as-measured — nothing is composited over transparency, there is no new text pair to solve; the risk moves entirely to NON-text (a soft same-colour edge can miss WCAG 1.4.11's 3:1), documented as the open adoption question rather than hidden. (2) *Saturation is still action* — the accent button is never rendered as monochrome clay; the skin touches only the neutral chrome hooks (`.glass-panel`/`.glass-well`/`.glass-rail`), and the brand plate stays a clean opaque stage (`.glass-plate`), so accent-state controls (sort tabs, switch track) never fight it on specificity. Base is the `--surface-sunken`/`--surface` token (not an invented colour) — one notch below `--surface` is what lets the white highlight read at all, and its contrast is already verified. Radius left untouched (token radii are tight on purpose; glass made the same call). Degradation: `prefers-contrast: more` + `forced-colors` collapse the clay back to a bordered opaque card; no `reduced-transparency` branch since nothing is translucent, and no no-JS dark fallback since `neu` is only ever reached through the client toggle (JS guaranteed), unlike the SSR-default glass. **Nothing entered `tokens.css`.** The rail switch is now `Glass / Quiet / Neu`; `Flat` was only ever the display label for the `data-skin="flat"` baseline (the "quiet chrome" direction), and `Quiet` names it correctly — internal id and selectors unchanged | src/app/demo/neu.css (new), src/app/demo/DemoLab.tsx
2026-07-22 | **Glassmorphism tried in `/demo` as a CANDIDATE skin (D24), not an adopted direction.** Owner asked to see the style applied to the lab. Rejected the obvious route first: `react-glass-ui` and every sibling library (`@liquidglass/react`, `@developer-hub/liquid-glass`, `shadcn-glass-ui`) are **live design sources** and D12 forbids exactly that — `shared/ui` is scaffolded then owned, and a package shipping its own glows, radii and shadows would re-open a decision the owners closed (`quiet chrome`). Several also carry heavy engines (Three.js, SVG displacement, html2canvas) — the same class of dependency stripped out of this page on 2026-07-22. Built it instead as ~200 lines of scoped CSS. **Three findings made the numbers, and they are the real output:** (1) *The field is bounded.* `--foreground-subtle` sits EXACTLY at the AA floor (4.54:1) by design and renders the trust badges, so the backdrop may depart from `--surface` by at most ΔL 0.08 before it breaks through 72% glass — constrain the BACKDROP, never dim the text. Falls out as a rule: subtle text may sit on glass, never bare on the field (4.29:1 there). (2) *Light is gamut-bound, not contrast-bound.* sRGB collapses toward white — at hue 46 the chroma ceiling is 0.056 at L 0.90 but 0.013 at L 0.975 — so a warm wash in the light theme is quiet BY PHYSICS. Light glass will always read subtler than dark here; that is the palette telling the truth, not a bug. (3) *Glass is chrome, not content.* Translucency edits what is behind it, which is precisely what ASK must not do to a business's own logo and brand colour, so the result card's brand layer is an OPAQUE island (`.glass-plate`) inside the glass card; the offer stays a tint and the primary action stays a solid accent fill (saturation is action — a translucent primary action is a weaker one). **66/66 measured contrast pairs pass** (light+dark × bare field / panel / well, text floor 4.5:1, ring floor 3.0:1); the solver and the verification table live in the file header. Ships with three degradation paths — `prefers-reduced-transparency`/`prefers-contrast: more` solidify it outright, `@supports not (backdrop-filter)` falls back to the opaque token, `forced-colors` yields to the user's palette — all three driven and verified in Chromium. The field is STATIC on purpose (an animated backdrop re-samples every pane above it every frame, and it sidesteps the transform/opacity motion lock). Two bugs found by driving it, both worth remembering: an opaque `bg-surface` on the wrapper painted OVER the `z-index:-1` field so the glass had nothing to refract; and the utilities must never set `border-radius` or touch accent-state controls — `[attr] .class` is specificity (0,2,0) and silently beats every `rounded-*` and `aria-pressed:bg-accent` utility. A `Glass / Flat` toggle in the rail flips `data-skin` so the two are comparable on identical markup. **Nothing entered `tokens.css`** — an unadopted experiment must not become the single visual source; if adopted, the values move there with a decision-log row | src/app/demo/glass.css (new), src/app/demo/DemoLab.tsx, src/app/demo/LabComponents.tsx
2026-07-21 | **`/demo` added as a permanent internal design lab (D24) — owner-approved exemption from the vision lock.** The visual system beyond tokens + the 9 primitives (colour in use, tone, composed components, widgets, motion, transitions) needs a place to be prototyped and AGREED before it reaches a product surface; the three surfaces that will set the tone — search form, result card, cabinet shell — are unbuilt, and prototyping inside a slice would ship unapproved UI to a real route. Placed at `src/app/demo/page.tsx`, deliberately outside BOTH `(marketing)` (else it is marketing content, D6) and `/app/*` (else placement silently gates or silently exposes it, D23); `robots: noindex`. Exempt from the "only what PRODUCT_VISION.md describes" lock and from i18n-keying (it is not a slice) — exempt from NOTHING else: the Design Locks bind unchanged there, since a pattern proven against relaxed rules would not survive promotion. A pattern agreed in /demo is rebuilt inside the slice owning its data; moving a file promotes nothing. No ESLint change needed (`**/src/app` is one element). Page is an empty placeholder by request — content follows the design discussion | src/app/demo/page.tsx, AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md (§2 + D24), AI_Knowledge/Locks.md (Product Locks)

2026-07-21 | **`dropdown-menu` primitive: bracketed boolean data-variants canonicalized (Tailwind lint hint).** IntelliSense `suggestCanonicalClasses`: `data-[highlighted]:`/`data-[inset]:` → `data-highlighted:`/`data-inset:` (Tailwind v4 shorthand for `&[data-highlighted]`/`&[data-inset]` — behaviour-identical). The VALUED variants (`data-[variant=destructive]:`, `data-[state=open]:`) correctly keep their brackets. Style-only, no visual or behaviour change; `eslint`/`prettier` clean | src/shared/ui/dropdown-menu.tsx

2026-07-21 | **Account menu: a divider splits the company page from the legal pages in "Learn more" (owner request).** `NavigationMenu`'s Learn-more group is split into `aboutItems` (About ASK) and `legalItems` (Terms/Privacy/Cookies), rendered through one `renderLearnMore` helper (removes the item-markup duplication across the two layouts; the desktop/mobile branch stays at the call site, never a caller-type flag — P6.3). Desktop fly-out gets a full `DropdownMenuSeparator` between the two groups; mobile's flat list gets an INSET one (`mx-2`) so it reads as a minor sub-group hint, lighter than the full-bleed section separators bracketing Settings/Log out — the owner asked for no divider or a minor one on mobile. Token-only (`bg-border` + spacing scale), no new strings, no invented UI (reorders existing UF 2.3 links). Verified in a real browser (production build): desktop fly-out + mobile flat, light AND dark, kk strings — all four screenshot-reviewed; `eslint src`/`tsc`/`prettier` clean; code-rules-checker passed | src/app/_components/NavigationMenu.tsx

2026-07-21 | **`useIsMobile` moved from NavigationMenu to `lib/` (rules-audit finding 4).** The viewport `matchMedia` hook is a domain-free cross-cutting browser utility, whose §6 decision-table home is `lib/`. Extracted verbatim (behaviour-preserving) to `lib/useIsMobile.ts` and imported by `NavigationMenu`, dropping the now-unused `useEffect`/`useState` imports. Left UNCHANGED with rationale: the `shared/ui` Badge `offer` variant (sanctioned by design locks D13/D14 as a visual register — carries no data/behaviour, already documented in the component). Verified: `tsc --noEmit`, `eslint`, `prettier --check` clean on the changed files. NOT run (needs an explicit build): `next build` + e2e — a verbatim hook extraction owes no visual pass | src/lib/useIsMobile.ts (new), src/app/_components/NavigationMenu.tsx

2026-07-21 | **Architecture authority corrected after the rules audit — §2 route tree completed, D18 locale detail superseded (docs-only).** The audit of the built code (the `auth` slice + app wiring + `shared`/`lib`) found NO code-level violations — boundaries R1–R6, single-implementation §7, P9.2 (zero raw hex/px, no `dark:` colour utilities, no direct `gsap` import), P1.1 sizes, and i18n key+P9.5 punctuation parity all clean. Two authority-doc drifts fixed, append-only per the CORE change protocol: **(1)** a **§2 note** documenting the `/oauth/callback` top-level route (shipped with Google OAuth but never entered in the §2 route tree — After-change §6); it is outside `(marketing)` and `app/` to match the backend redirect URI, a thin server route over the `@/auth` client island, static shell, unaffected by the rendering-contract check. **(2)** a **§11 correction** flagging D18's "the marketing landing stays ru" as superseded by the already-approved 2026-07-20 default-locale `ru`→`kk` decision (the landing server-renders kk now) — a factual correction propagating an approved decision into the authority, not a change to D18 itself (After-change §7). No code changed | AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md (§2 + §11 notes)

2026-07-21 | **Platform route guards — the /app gate (owner directive, rules 1–5; decision-log D23).** The platform is now gated, ahead of the search/profile slices that had parked it. **Rule 2** (logged-out cannot enter `/app/*`): `RequireAuth` (new, `@/auth`) wraps the `(main)` route group's layout and redirects to `/app/auth/login`; the auth pages are exempt by SITTING OUTSIDE `(main)` — the route-group boundary is the gate line, not a per-URL allowlist. **Rule 1** (customer-only cannot open the Dashboard): `RequireDashboardAccess` (new) in a new `business/layout.tsx` redirects a customer to `/app`; it and the nav share ONE predicate, `canAccessDashboard(user)` (new, `auth/model.ts`), so the hidden link and the blocked route can never diverge (P6.2). Both guards are **client-side** — the token is localStorage-only (D5/D6), invisible to server/middleware; a server guard waits on the Phase-4 cookie migration — and show a `GuardFallback` spinner while the session restores, NEVER redirecting before it settles (else every load bounces authenticated users to login). **Rule 4**: the nav's signed-out "sign in" button + `app.nav.signIn` key were REMOVED — the nav only mounts inside the guard, so no signed-out state exists. **Rule 3**: `/terms`, `/privacy`, `/cookies` were built as static, content-only routes under `app/(marketing)/` (OUTSIDE `/app`), clearing the register-agreement 404s (ROADMAP parked fixes); bodies are a neutral "being prepared" placeholder — legal copy is owner-authored, never invented (P9.1). **Rule 5**: `LandingRedirect` (new `(marketing)` client island) carries a logged-in visitor from `/` to `/app` unless `?from=app` (D6), reading the token in an effect (not `useSearchParams`) so `/` stays static. **Trade-off recorded in D23 (amends D7):** Home/Catalog/Product are now behind the gate, so they are NOT public/SEO-crawlable in V1 — narrowing D7's "public surfaces" to the marketing landing + legal pages (public city/category SEO pages remain a Phase-2 item, so nothing shipping is lost). i18n: removed `app.nav.signIn`, added `app.loading` + `marketing.legal.*` across en/kk/ru. e2e: new `guard.spec.ts` (the rule 1/2 redirect matrix); `smoke.spec.ts` + `navigation.spec.ts` reworked (platform routes now seed a session; the signed-out test asserts the login redirect, not a sign-in link); `design-system.spec.ts` repointed from `/app` to the ungated `/app/auth/login`. NOT run (needs an explicit "run"/"build"): `next build` + the e2e suite | src/auth/model.ts, src/auth/index.ts, src/auth/ui/{RequireAuth,RequireDashboardAccess,GuardFallback}.tsx, src/app/app/(main)/layout.tsx, src/app/app/(main)/business/layout.tsx (new), src/app/_components/NavigationMenu.tsx, src/app/(marketing)/{page,LandingRedirect,terms/page,privacy/page,cookies/page}.tsx, src/shared/i18n/messages/{en,kk,ru}.json, e2e/{guard,smoke,navigation,design-system}.spec.ts, AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md (§2/§7/D23), AI_Knowledge/Locks.md, AI_Knowledge/features/auth/{README,ux-ui-flow,locks}.md, AI_Knowledge/ROADMAP.md

2026-07-20 | **Default locale set to `kk` (Kazakh) — owner decision.** `defaultLocale` in `shared/i18n/locales.ts` moved `ru` → `kk` (locales reordered kk-first). ASK is a Kazakhstan product (ask.com.kz), so Kazakh is the fallback when no `ask.locale` cookie is stored — the static marketing landing now server-renders kk (D6), and `/app/*` renders kk before a client switch (D18/D19). Only the DEFAULT changed: the ru/kk/en switcher still overrides per session, and the "no request-time cookie on the static landing" mechanism is unchanged. Supersedes the 2026-07-14 note that pinned the default to `ru` until profile settings. Reconciled the ru-assuming e2e assertions to kk (`auth.spec.ts`: login heading, role-selection + inactive-account error copy; the switch test is now kk→en) and the ROADMAP i18n note. Surfaced by CodeRabbit review of ce8a3d8 | src/shared/i18n/locales.ts, e2e/{auth,navigation}.spec.ts, AI_Knowledge/ROADMAP.md

2026-07-20 | **i18n copy punctuation made consistent — sentences terminate, fragments don't (owner discussion → new rule P9.5).** The message catalog had drifted: most `auth.errors.*` end in a period ("Enter a valid email address.", "Passwords do not match.") but four were authored period-less in every locale (`passwordRequired`, `invalidCredentials`, `accountNotActive`, `nameRequired`) — full sentences dressed as fragments, the only real inconsistency in the file. Decided to terminate all of them rather than strip periods, because several error strings are TWO sentences ("Something went wrong. Check your connection and try again.") whose required internal period makes a missing trailing one read as truncation — so terminating every sentence is the only internally consistent choice. Added the four terminal periods across en/kk/ru (12 strings, locales stay punctuation-parallel) and codified the convention as **P9.5**: system-spoken strings (errors, validation, notifications, status) end in `.`; UI chrome (labels, buttons, titles, subtitles, placeholders, nav) carries none. Docs-only decision beyond the copy; no code logic touched | src/shared/i18n/messages/{en,kk,ru}.json, AI_Knowledge/DESIGN_PATTERNS_FRONTEND.md (P9.5)

2026-07-19 | **Second backend sync — AskBackend "Final Major Update" (`dev` @cf26320) + config defaults (@4d83005); docs-only.** The backend's `dev` moved two commits past the V2 sync. Frontend-facing deltas written down: **(1) The OAuth bridge is now IMPLEMENTED — exactly the option-2 hand-off we specified.** `GET /api/v1/auth/session` accepts a Bearer token OR the single-use `ASK_SESSION` bridge cookie; under the cookie it validates a revocable server session, returns a signed **HS256 JWT** (`access_token`/`token_type`/`expires_in`), and **clears the cookie**. So our "open backend blocker" is DONE and the auth-slice OAuth work is fully unblocked; recorded the confirmed exchange + the new `expiresIn`/`expires_in` field in `features/auth/{contracts,ux-ui-flow,README}.md`, and flipped the ROADMAP cross-repo row + auth-follow-up item to done. `GET /session` now returns a REAL `access_token` under the bridge cookie (still null under a Bearer token). **(2) `catalogScope` = PRODUCTS/SERVICES/BOTH** threads through seller onboarding (`/seller/onboarding` request), managed-import requests (require `catalog_scope` + source types/details/contact/legal), and catalog capabilities (platform grant is now a scoped PRODUCTS-or-BOTH grant); new `GET /platform/managed-imports/businesses/{id}/catalog-access`; a 4-step seller catalog-onboarding UX — recorded in `features/{business-cabinet,catalog}/contracts.md`. **(3) Config defaults committed:** `OAUTH2_FRONTEND_REDIRECT_URI` → `http://localhost:3000/oauth/callback`, `ASK_FRONTEND_BASE_URL` → `:3000` (resolves the drift we flagged). **(4) G1 reconciled (your call):** the gate + its cross-repo row described the dead `UnifiedSearchRequest`; the new `POST /api/v1/search` delivers relevance/distance/cost sorts + price/city/category filters, so G1 is now **PARTIAL** — only the Unique-Offers sort, a Companies filter, and 100 km-radius / map-area location filters remain. Backend also squashed migrations V1–V19 into one Flyway baseline (fresh-deploy; not frontend-facing). No code changed | AI_Knowledge/ROADMAP.md (G1 + cross-repo + auth follow-up), AI_Knowledge/features/auth/{contracts,ux-ui-flow,README}.md, AI_Knowledge/features/{business-cabinet,catalog}/contracts.md

2026-07-19 | **Owner directive: Google OAuth is REQUIRED on the Log in and Sign up pages; the "Email-only auth" lock is reversed (docs-only — no code yet).** Two gates that had parked OAuth are cleared by explicit owner approval: (1) **vision** — appended an "Auth methods" note to `PRODUCT_VISION.md` UF 1 (a CORE authority — append + date + justification, no rewrite): the Authorization Page offers email+password AND "Continue with Google", both on login and register; email+password alone is no longer sufficient; SMS/phone stays out of V1. (2) **lock** — `features/auth/locks.md` "Email-only auth for MVP" is reversed to "email+password AND Google OAuth, both required" (owner approval is the recorded requirement-1 for breaking a lock); added a second lock fixing the auth model: **OAuth bridges to a Bearer token, never cookie-native** (the `ASK_SESSION` cookie is only the OAuth bootstrap; the token lock/`ask.accessToken` holds, cross-platform D5). **Hand-off DECIDED — option 2 (cookie→token exchange):** `CorsConfig.java` confirms `allowCredentials(true)` on `/api/**` and the prod cookie is `SameSite=None;Secure`, so `/oauth/callback` will call `GET /session` once with `credentials:'include'` and pipe the result through the existing `applySessionTo` → `startRoute` path — an OAuth login becomes identical to a password login. Rejected the fragment hand-off (token in history) and cookie-native auth (breaks the token lock + RN path). Documented the button placement (a secondary action under the email form, never the accent fill — saturation-is-action holds), the transient callback states (spinner / error→back-to-login), and `suggestRoleExpansion` arming the role modal just like verify, in `features/auth/{contracts,ux-ui-flow,README}.md`. Roadmap gained an "Auth follow-up" item (route + button + `credentials` option) and two cross-repo rows: the **open** backend task (`/session` must return `access_token` under cookie auth) and the per-environment redirect-URI/Google-Console/client-secret setup (dev/prod defaults already aligned to `:3000`/`ask.com.kz`; stage must override). Implementation pending the backend session-token change | AI_Knowledge/PRODUCT_VISION.md (UF 1), AI_Knowledge/features/auth/{locks,contracts,ux-ui-flow,README}.md, AI_Knowledge/ROADMAP.md

2026-07-19 | **Synced consumed-contract docs to AskBackend "Major Update V2" (backend `dev` @56f88f8, 2026-07-18); no frontend code touched.** Cross-project rule (CLAUDE.md "Cross-project awareness"): a backend contract change updates our matching `features/{slice}/contracts.md`. The backend's global snake_case switch is already absorbed by `shared/api/caseTransform.ts` (D20), so the wire change needs no code — but six contract docs drifted from what the backend now serves. Synced: **search** — rewrote from the stale `/api/v1/search/unified` + `UnifiedSearchRequest` shape (no longer on the backend) to `POST /api/v1/search` with pagination (`page`/`page_size`/`total`/`has_next`), `filters`+`overrides` (explicit wins), `EXACT` vs `ALTERNATIVE` sections + `relaxed_constraints`/`reason`, `interpreted_constraints`, card `availability_warning`/`match_reasons`, and `diagnostics` (never rendered); sort is now `intent_match`(default)/`distance`/`price_asc` — `price_desc` removed, `distance` added; Meilisearch is the retrieval engine, PG hydration+fallback. **chats** — file attachments (`/chat/upload`, `/chat/files/{name}`, 10 MB + whitelist), `conversationType` (GENERAL_SUPPORT|MANAGED_IMPORT), `status` (PENDING→IN_CHAT→CLOSED), `senderType` +PLATFORM, `attachmentUrl`, CONVERSATION_CLOSED. **catalog** — new `/catalog/capabilities` set (business members = MANUAL_PRODUCT_EDIT+EXCEL_IMPORT only), Excel import +PLATFORM role, and **AI Autodump is now platform-only** (business cabinets lost it — build no Autodump control). **business-cabinet** — business-scoped members/invitations, `/seller/onboarding`, `/catalog-setup` status, `BranchDto.addressDetails`, new INVITATION_*/BUSINESS_MEMBER_*/MANAGED_IMPORT_* errors. **profile** — `DELETE /api/v1/account` (409 ACCOUNT_OWNER_TRANSFER_REQUIRED for a sole owner), account export removed, and fixed a stale `phone` field (removed from AppUser in backend V8). **auth** — `change-password` body clarified (`currentPassword`+`newPassword`), non-enumerating login-start noted, and **Google OAuth documented as GATED**. **Two owner decisions flagged, not acted on:** (1) the backend added a whole `platform` staff cabinet (`/api/v1/platform/*`) — the frontend Feature Index has no platform slice and our `auth/model.ts` states platform roles have no V1 surface (P9.1); left as a pointer, no slice created. (2) Google login is absent from PRODUCT_VISION and collides with the `Email-only auth for MVP` auth lock — the `/oauth/callback` + cookie→Bearer plan is drafted but blocked on owner sign-off + a backend token-handoff contract. Docs-only change; no build run | AI_Knowledge/features/{search,chats,catalog,business-cabinet,profile,auth}/contracts.md

2026-07-19 | **Navigation menu made mobile-first (owner report: "breaks when shrunk" + the Learn-more fly-out broke on narrow/touch).** Two responsive changes to `app/_components/NavigationMenu` (platform-ui-design §7): (1) the destination chips (Home/Chats/Dashboard) collapse to **icon-only** buttons below `sm` (lucide Home/MessageCircle/LayoutDashboard, 44px targets, `aria-label` carries the name), labels return at `sm+` — pure CSS (`sm:hidden` / `hidden sm:inline`), so no overflow or cram regardless of label length or locale (English "Dashboard", long ru strings no longer collide; the old text chips only "fit" at 360px because the ru words were short). (2) the account menu's "Learn more" is the hover/keyboard **fly-out submenu on desktop** but a **flat inline group on mobile** (a `useIsMobile` matchMedia hook, `max-width:639px`): a sideways fly-out has neither the room nor the hover a phone needs — it opened to nothing (the reported break). The four links (About ASK, Terms, Privacy, Cookies) render from one `learnMoreItems` array in both branches (DRY); the mobile group carries a "Learn more" section label. **No hamburger** — with only 2–3 destinations, hiding them behind a tap costs discoverability for no space gain (owner asked; rationale recorded in the component doc). SSR-safe: chips are CSS-only, and the submenu structure only mounts when the menu opens (post-hydration), so `useIsMobile` starting false cannot mismatch. Verified: `npm run build` green (rendering contract intact); `eslint src` / `tsc` / prettier clean; `e2e/navigation.spec.ts` asserts the per-viewport layout (fly-out present + hovered on desktop, absent on mobile via `toHaveCount(0)`) rather than adapting to whatever rendered — a CodeRabbit nitpick — **12/12 across chromium + mobile-chromium** (the Pixel 7 project now exercises the flat layout); mobile bar + mobile account menu + desktop fly-out screenshot-reviewed | src/app/_components/NavigationMenu.tsx, e2e/navigation.spec.ts

2026-07-18 | **Platform navigation menu built — the UF 2.1–2.3 chrome, from a structure-only stub to the real bar (owner design discussion).** `app/_components/NavigationMenu` is now the sticky platform bar. LEFT: the ASK mark (new `app/_components/AskMark` — the favicon artwork inlined so it inherits `currentColor`) links home in the brand **accent** via `text-accent` (owner decisions: icon over wordmark for the platform, and orange over ink — a logo is brand identity, the one sanctioned accent use beyond an action; the primary/search control stays the only accent UI *affordance*, so saturation-is-action still reads; the accent token carries its own light/dark values so the mark stays orange in both with no `dark:`; the wordmark leads the marketing landing). MIDDLE: Home + Chats, plus a role-gated Dashboard shown only when `user.kind !== "customer"` (seller/staff, UF 3.1), keyed off `useAuth` (R6). RIGHT: the account card (initials avatar — the backend returns NO avatar field, so initials from displayName, never invented, P9.4 — + name) opening the profile-card dropdown from UF 2.3: Settings (→/app/profile), Learn more ▸ [About ASK (→ landing at `/?from=app`, the query suppressing the D6 logged-in redirect so the landing stays reachable), then Terms, Privacy, Cookies] — all nested Claude-style under Learn more, each with its own page icon in one aligned column (owner request), Sign out (`useAuth().signOut()` → /app/auth/login). **History deliberately OMITTED** — it belongs in the search field's "previous requests" dropdown, not the nav (kept us out of a P9.1 invention: no History flow exists). **Cookies is a NEW legal route** (owner-approved — ASK stores theme+locale in cookies, D19); its page + Terms/Privacy ship with the landing (roadmap #10), so the three links resolve to placeholders/404 until then — a known stub, same status as the register agreement links. Three mandatory states (P8.4): loading→Skeleton, signed-out→Sign in link, authenticated→the card. New shared/ui primitive **dropdown-menu** scaffolded from shadcn/Radix and restyled to tokens BEFORE first use (D12, the 10th primitive after D14's nine): highlighted rows use `bg-surface-sunken` not shadcn's `focus:bg-accent` (the accent is never a hover fill), tw-animate enter/exit dropped (no second motion system, §7), Sub* parts carry the Learn-more submenu. i18n `app.nav.{aria,brand,dashboard,signIn}` + `app.userMenu.*` across ru/kk/en (unused `nav.{profile,business}` removed). The account card is app-chrome SCAFFOLDING; the profile-card content (real settings, avatar image) belongs to the `profile` slice (roadmap #6) and slots into the Settings target with no nav change. Verified: `npm run build` green (rendering contract intact — `/` static, /app/* dynamic); `eslint src`/`tsc`/prettier clean; new `e2e/navigation.spec.ts` (signed-out/customer/seller states, dropdown items, dark theme, sign-out clears the token) passes on chromium + mobile; light+dark screenshots reviewed against the platform-ui-design Definition of done | src/app/_components/{NavigationMenu,AskMark}.tsx, src/shared/ui/dropdown-menu.tsx (new), src/app/app/(main)/layout.tsx, src/shared/i18n/messages/{en,ru,kk}.json, e2e/navigation.spec.ts

2026-07-18 | **D6 static-landing lock was violated — the build shipped `/` as ƒ (dynamic), not ○ (static); fixed with `setRequestLocale`.** An architecture/design audit (owner-run) caught that the production route table marked EVERY route dynamic, `/` included, contradicting D6/D18/D19's repeated guarantee that the SEO marketing landing stays statically rendered. Root cause: next-intl treats `getLocale`/`getMessages`/`getTranslations` as dynamic until the request locale is SEEDED — and the root layout + AppProviders + the landing page all call them with no seed, so the whole tree opts into dynamic rendering. `request.ts` not reading a cookie is NECESSARY but not SUFFICIENT (the comment there overclaimed "the landing stays static"). Fix per next-intl's static-rendering contract: `setRequestLocale(defaultLocale)` at every static entry point — the root layout and the landing page (next-intl requires it at each entry, not only an ancestor). The server always serves `defaultLocale`; the /app locale switch is client-side (D18) and the platform layout still reads cookies, so `/app/*` stays dynamic exactly as D19 intends. Proven by rebuild: `/` and `/_not-found` → ○ (Static), all `/app/*` → ƒ (Dynamic); `npm run build` (eslint → lint:fixtures → tsc → next build) green, `/` prerendered without error. No decision reversed — the code now MEETS D6. **Guarded going forward (CodeRabbit follow-up):** `scripts/check-rendering.mjs` (new `verify:rendering`, appended to the `npm run build` chain AFTER `next build`) asserts the D6/D19 contract against `.next/prerender-manifest.json` — `/` static, no `/app*` static — so a future unseeding fails the build instead of silently shipping. Proven both ways like the lint fixtures (green on the real build; exit 1 on a manifest with `/` dropped or `/app` made static); §8 note dated 2026-07-18 | src/app/layout.tsx, src/app/(marketing)/page.tsx, src/shared/i18n/request.ts, scripts/check-rendering.mjs, package.json, AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md §8 (2026-07-18 note), AI_Knowledge/AUDIT_0.md

2026-07-18 | **xhigh code review of the last 5 commits (10 finder angles → verification → sweep; 14 findings), then owner-approved "fix them all".** The dominant defect class: the 2026-07-17 storage-unavailable fix was special-cased in ONE caller (LocaleProvider's `sessionLocale`) while its siblings on the same door stayed exposed. Fixed at the door — decision-log row **D22**: `shared/api/storage.ts` gained an in-memory write-through fallback (a dropped write reads back in-session — so a login in a storage-blocked browser keeps its Bearer header and the theme toggle keeps the picked option instead of snapping to "system" and being overwritten by the next OS flip) and `storage.subscribe(key, cb)` wrapping the native `storage` event; `sessionLocale` retired. Cross-tab sync wired on it: AuthProvider re-runs its restore (with a stale-response token guard) so sign-out/login in one tab syncs the others; theme re-applies `data-theme`; locale re-notifies. Transport: `snakify()` now matches Jackson's SnakeCaseStrategy on acronym runs (`userID` → `user_id`, was `user_i_d` — the D20 silent-null class, latent); httpClient's success path no longer throws a raw SyntaxError on a 2xx empty/non-JSON body (both throw the structured ApiError — §7 one-error-type; never `undefined as T`, a bodiless success is 204); `formatPageTitle` uses a replacer function (a `$&`-bearing translated title would corrupt the tab title); `ERROR_KEY_BY_CODE`'s wrong-context `INVALID_CREDENTIALS→codeInvalid` row now maps to `errors.invalidCredentials`. Auth UX: `RoleSelectionModal` mount moved `(main)` → platform layout (browser-back to `/app/auth/*` escaped the undismissable choice); register's `setField` clears the passwordConfirmation mismatch when the password field is edited to agree; every Field error carries `{inputId}-error` + `aria-describedby`/`aria-invalid` wiring (screen readers re-discover the error from the field); RoleCard classes via `cn()`; LocaleProvider context value memoized. Theme rendering: `tokens.css` gained the no-JS dark fallback (`@media (prefers-color-scheme: dark) { :root:not([data-theme]) }` — a comment-bound sanctioned duplicate of the dark block, reachable only when scripts never run; header §note 2026-07-18); the Toaster moved `app/providers` → platform layout INSIDE the `ask.theme` seed and its SSR snapshot derives from the new `useThemePreferenceSeed()` (explicit dark hydrates dark, D21/D19; the root tree must never read the cookie, D6). Same-day review follow-up on the fixes themselves: AuthProvider subscribes BEFORE the initial restore (a token change during that async window must re-trigger, not slip through), and a `storage` event drops the key's in-session fallback before notifying (the event proves localStorage is the fresher truth). Review candidates REFUTED with evidence, for the record: verify-step `requiresRoleSelection` (backend `verifyCode` always resolves a user from the challenge — the fork happens in `login()` before the 2FA challenge exists), snake_case `detail.field` values (backend sends Java camelCase via `fieldError.getField()`), and several duplications sanctioned by P6.4. Verified: `tsc --noEmit`, `eslint src`, prettier clean. NOT run (needs an explicit "run"/"build"): `next build` + e2e — the modal/Toaster mount moves and the no-JS fallback await that pass | src/shared/api/{storage,caseTransform,httpClient}.ts, src/shared/{theme.ts,config/site.ts}, src/shared/i18n/LocaleProvider.tsx, src/shared/ui/{sonner,theme-toggle}.tsx, src/auth/hooks.ts, src/auth/ui/{AuthProvider,Field,LoginForm,RegisterForm,VerifyCodeForm,PasswordInput,RoleSelectionModal}.tsx, src/app/app/layout.tsx, src/app/app/(main)/layout.tsx, src/app/providers/AppProviders.tsx, src/design-system/tokens.css, AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md §11 (D22), AI_Knowledge/features/auth/{README,ux-ui-flow}.md

2026-07-17 | **Review follow-ups on the rules-audit fixes (two robustness gaps + comment drift).** (1) **`shared/api/storage.ts` no longer trusts localStorage to exist**: disabled site data, sandboxed iframes and some private modes throw on ACCESS, not only on write — every method now degrades (null read, dropped write) instead of crashing the caller, matching the pre-paint theme script's own try/catch. (2) **LocaleProvider reconciles the server-seeded locale with client storage**: the client snapshot previously fell back to `defaultLocale` when localStorage was empty/blocked, which flipped a cookie-remembered language back to ru after hydration AND then stomped the good cookie via the sync effect. Now the stored value wins when present, otherwise the server-seeded `initialLocale` (the `ask.locale` cookie, D19); the sync effect mirrors the resolved locale into BOTH stores. Second pass, same day: an in-memory `sessionLocale` copy (preferred by the snapshot) keeps a SWITCH active when persistence is unavailable — without it, `setStoredLocale`'s dropped write meant the snapshot re-read nothing and snapped back to the server seed, freezing the switcher. (3) The owner split the modal's compact step into a second token — `--breakpoint-xxs: 24rem` (header type) beside `--breakpoint-xs: 30rem` (grid collapse); the tokens.css and RoleSelectionModal comments now describe both steps. Verified: eslint, tsc, format:check green | src/shared/api/storage.ts, src/shared/i18n/LocaleProvider.tsx, src/design-system/tokens.css, src/auth/ui/RoleSelectionModal.tsx

2026-07-17 | **Rules-audit fixes (P-rules + §7), owner-approved "fix them all".** A full audit against ARCHITECTURE_PATTERN_FRONTEND + DESIGN_PATTERNS_FRONTEND found no R1–R5 violations; nine code-level findings, all fixed: (1) **D15 break** — `RoleSelectionModal.tsx` had shipped unformatted in e66d89e (CI `format:check` red); formatted. (2) **§7 i18n** — Spinner's hardcoded default `label = "Загрузка…"` removed; `label` is now REQUIRED and caller-translated (every caller already passed one — the default was also dead weight, P8.1). (3) **P5.2** — the auth pending-role flag read/wrote `window.localStorage` directly in slice logic; new `shared/api/storage.ts` (the generic key-value door; `tokenStorage` rebased on it, `theme.ts`/`LocaleProvider` routed through it too — ONE server-guard) and a single `persistPendingRoleSelection(store, pending)` helper replaces the 5 repeated "write storage + set store" pairs (P6.2). (4) **§7 theme / D21** — the Toaster resolved the OS directly (`theme="system"`, a D17 leftover); it now subscribes to the resolved `data-theme` via new `getResolvedTheme()` (see decision-log row D21; CLAUDE.md/AGENTS.md/ROADMAP stack lines corrected). (5) **P6.2 locale** — the "stored value → validated Locale" logic existed in 5 places; now `parseLocale()` (locales.ts) + `localeFromCookies()` (new `shared/i18n/serverLocale.ts`, used by the platform layout and both auth generateMetadata blocks); same for the theme parse → `parseThemePreference()`. (6) **P6.2 title** — the "%s - Ask" template knowledge lived in 3 places; now `shared/config/site.ts` (`TITLE_TEMPLATE`/`formatPageTitle`), consumed by the root metadata and both `document.title` effects. (7) **Magic breakpoints** — `max-[400px]`/`max-[480px]` in RoleSelectionModal replaced by ONE token step `--breakpoint-xs: 30rem` (tokens.css) used as `max-xs:`; deliberate small visual delta: the modal header now compacts at 480px (with the grid collapse) instead of 400px. (8) **globals.css header contradiction** — it claimed "nothing here may introduce a value" while defining the 2px/3px focus geometry; the header now states the one exception explicitly (the signature is defined once beside its utilities, P6.1; colour still from --ring). (9) **P3.1** — PasswordInput's `onChange` kept the native name with changed semantics (string, not event); renamed `onValueChange`. Verified: `eslint src` clean, `lint:fixtures` all proven, `tsc --noEmit` clean, `format:check` clean. NOT run (needs an explicit "run"/"build"): `next build` and the e2e suite — the sonner theme subscription and the modal's 480px step await that visual pass | src/shared/api/{storage.ts (new),tokenStorage.ts}, src/shared/{theme.ts,config/site.ts (new)}, src/shared/i18n/{locales.ts,serverLocale.ts (new),request.ts,LocaleProvider.tsx}, src/shared/ui/{spinner,sonner}.tsx, src/auth/hooks.ts, src/auth/ui/{AuthProvider,LoginPage,RegisterPage,LoginForm,RegisterForm,PasswordInput,RoleSelectionModal}.tsx, src/app/layout.tsx, src/app/app/layout.tsx, src/app/app/auth/{login,register}/page.tsx, src/app/globals.css, src/design-system/tokens.css, AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md §11 (D21), AI_Knowledge/ROADMAP.md, CLAUDE.md, AGENTS.md

2026-07-17 | **Password visibility toggle (owner request): an in-field eye on every password field.** New slice-private `PasswordInput` (D8 rule of three — auth is the first consumer, same call as the agreement checkbox): the shared Input with `pe-11` end padding plus an absolutely-positioned 44px toggle (`Eye`/`EyeOff`, `text-foreground-muted`, focus-ring; label flips show/hide per the WAI pattern — no aria-pressed). **Register's password + confirmation share ONE visibility state — either eye reveals/hides BOTH fields together** (the owner's "connected" requirement); login owns a single state. Visibility is controlled by the caller precisely so that sharing is possible (P5.1-style lifted state, not component-internal). Verified live in light (register pair) and dark (login) — both flip `type` correctly; e2e scenario added asserting the pair flips together from either eye and login flips independently. i18n `fields.showPassword`/`hidePassword` in ru/kk/en (51/51/51 parity) | src/auth/ui/{PasswordInput,RegisterForm,LoginForm}.tsx, src/shared/i18n/messages/{en,ru,kk}.json, e2e/auth.spec.ts, AI_Knowledge/features/auth/ux-ui-flow.md

2026-07-17 | **RoleSelectionModal redesigned: persistent, non-dismissable, card-based (owner request + reference image + the owner's Vue design patterns).** (1) **The modal now follows the SESSION, not a page**: `useVerifyStep` arms `ask.roleSelectionPending` (localStorage) + a store copy when verify returns `suggestRoleExpansion`; the page navigates to `startRoute` and the modal — mounted by the `(main)` layout, self-driven via `useRoleSelection` — opens OVER `/app` and survives navigation and reload until answered. Dismissal is impossible by design (no close button — `showCloseButton={false}` —, ESC and outside clicks prevented); the flag dies only with an answer or the session (sign-out / token rejected). Pattern lifted from the owner's TeacherInvitationModal reference: pending-state keyed in localStorage, cleared by the explicit answer. (2) **Anatomy per the reference image**: two-card radiogroup (icon badge + label + hint, roving tabindex + arrow keys), customer preselected (search is the mission), one full-width accent Continue. Selected card = accent border over `accent/10` tint — saturation-is-action holds, the tint stays low-chroma. (3) **Register's name field is now REQUIRED — a backend accommodation, not product churn**: live verification exposed that `app_user.display_name` is NOT NULL while the DTO marks it optional, so a nameless registration always died at verify with 409 `DATA_CONFLICT` that the UI could only phrase as "code invalid" (this had masked itself as a modal-redesign regression). Raised with backend; relax when DTO and schema reconcile. Verified on the LIVE stack (headless Chromium, real backend): no-dismiss ✓ reload persistence ✓ business → /app/business ✓ mobile ✓ dark+kk ✓; light/dark/mobile screenshots reviewed against the platform-ui-design Definition of done. e2e: the register flow test reworked (modal now asserted over /app) + a dedicated no-dismiss/reload-persistence scenario | src/auth/{store,hooks,model}.ts, src/auth/ui/{RoleSelectionModal,RegisterPage,RegisterForm,AuthProvider}.tsx, src/app/app/(main)/layout.tsx, src/shared/i18n/messages/{en,ru,kk}.json, e2e/auth.spec.ts, AI_Knowledge/features/auth/{README,contracts,ux-ui-flow}.md

2026-07-17 | **D20 — the backend wire is snake_case; httpClient now owns the case boundary. Found by running the REAL stack, provable no other way:** the first live register call 400'd because `spring.jackson.property-naming-strategy: SNAKE_CASE` (backend base application.yml, on every deploy branch since 2026-06-25) made camelCase request fields land `null` server-side — the Phase-0a "backend speaks camelCase, no transform needed" note had verified Java FIELD NAMES, never the wire. Fix per the data authority (D9/P9.4): new `shared/api/caseTransform.ts` (pure deep key mappers), applied only inside `httpClient` — request bodies snakify, response bodies AND error bodies camelize (before this, `ApiError.errorCode` was silently `undefined` against the real backend, so ALL precise error messages fell back to generic), query params untouched. Slices stay 100% camelCase. e2e stubs converted to the real wire so the boundary is exercised in every scenario; the wrong-code stub also corrected to the real `CHALLENGE_INVALID_CODE`. **Live smoke against the running backend (local profile, JDK 21): register 201 → verify 200 (staging-bypass) with `suggest_role_expansion=true` → session restore (`access_token` null, bare-enum role) → password login 200 → wrong password 401 `INVALID_CREDENTIALS` → logout → revoked-token session = 403 (confirming the AuthProvider 401/403 clear-set) → duplicate register 409 `EMAIL_ALREADY_REGISTERED`. The predicted `user.status="PENDING"` snapshot cosmetic in the verify response confirmed live** | src/shared/api/{caseTransform,httpClient}.ts, e2e/auth.spec.ts, AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md §11 (D20), AI_Knowledge/ROADMAP.md (0a note), AI_Knowledge/features/auth/contracts.md

2026-07-17 | **Auth hardening after a line-by-line re-verification against the backend identity source** (controller, both processors, exception handler — the contract matches; these were the behavioral gaps). (1) **The `requiresRoleSelection` intermediate is now handled**: `POST /auth/login` on a multi-role account returns 200 with NO user, NO token, NO startRoute; before, it fell into the success branch — an empty session was applied and the page navigated to `/app` signed OUT, a silent failure. Now it is a form-level error (`auth.errors.roleSelection`, ru/kk/en); `select-role` stays deferred (roadmap #7), and deferred means an honest error, never a fake success (P9.4, P9.3). (2) **`ACCOUNT_NOT_ACTIVE` (403, blocked/deleted account) gets its own message** instead of the misleading "network" fallback — one `ERROR_KEY_BY_CODE` row + `auth.errors.accountNotActive`. (3) **Session restore no longer treats every failure as a bad token**: only 401/403 (the backend rejecting the token) clears `ask.accessToken`; a network failure or 5xx keeps it for the next load, so an offline app-open no longer signs the user out permanently. (4) **`displayName` typed `string \| null`** on `AuthUserResponse`/`AuthUser`/`RoleOption` — registration accepts an empty name and the backend stores null; the type now tells the truth (P9.4). Also fixed a STALE doc line: the auth README still listed password login as deferred (leftover from the pre-2026-07-16 OTP design). Two e2e scenarios added (role-selection intermediate → error, no token, no navigation; inactive account → its own message) | src/auth/hooks.ts, src/auth/model.ts, src/auth/ui/AuthProvider.tsx, src/shared/i18n/messages/{en,ru,kk}.json, e2e/auth.spec.ts, AI_Knowledge/features/auth/{contracts,ux-ui-flow,README}.md

2026-07-17 | **Focus/checkbox polish (owner report): squared focus corners fixed at the root; the ring and the check now transition.** (1) **The `focus-ring` utility carried a bug**: `border-radius: inherit` inside `:focus-visible` was meant to make the outline "hug" the element but actually OVERRODE the element's own radius on focus — every focused Input snapped to square corners (the reported glitch), since it inherited the parent's 0. Outlines follow the element's radius natively; the line is gone. (2) **The ring now fades in**: the utility keeps a transparent 2px outline at rest and `:focus-visible` changes only its colour — so any element with `transition-colors` (which covers `outline-color` in Tailwind v4) animates the ring. `outline-none` was removed from Input/Button/Select-trigger (it fought the resting outline); `transition-colors` added to the focus-ring carriers that lacked it (auth links, Badge); the dialog close button's `transition-opacity` widened to `transition`. (3) **The agreement checkbox animates**: the native `accent-color` checkbox (which cannot transition) became an `appearance-none` token-styled box + a peer'd lucide Check that fades/scales in; checked fill is the accent — the box is an actionable control, so saturation-is-action holds; `aria-invalid` shows the destructive border like Input. Reduced motion is already gated globally in globals.css. (4) **Field fade re-tuned on owner review**: Input and the Select trigger ease their colour transitions (ring, invalid border) over `duration-(--duration-base)` (220ms) with the token `ease-out` curve — the 150ms default still read as a pop; buttons/toggles keep the snappier default. (5) **Field focus re-designed on further owner review — the signature gains a FIELD form.** On a bordered field the offset outline read as two detached rectangles ("old and cheap"); the new `focus-ring-field` utility instead turns the field's own border to the ring colour and blooms a soft 3px halo (`color-mix` of --ring at 25%) out of it, hugging the radius with no gap — the resting --shadow-xs stays in the focused shadow list so depth doesn't pop. Defined ONCE in globals.css beside `focus-ring` (P6.1 holds: fields use the field form, chrome keeps the offset ring; both draw from --ring). Input + Select trigger switched; their transition lists name box-shadow so the halo animates. Refines the D14 "no blur, no glow" phrasing with owner approval — the halo is the field form of the one signature, not a second system. Correction same day: the utility must also carry a transparent resting outline — without it the UA focus ring painted a dark outline OVER the halo (read as a detached gap); transparent rather than `none` keeps a visible ring in Windows forced-colors mode | src/app/globals.css, src/shared/ui/{input,button,select,badge,dialog}.tsx, src/auth/ui/{RegisterForm,LoginPage,RegisterPage}.tsx

2026-07-17 | **D19 follow-up: `request.ts` was swallowing the explicit locale — the title stayed ru despite the cookie.** The D19 wiring passed `getTranslations({ locale })` from the auth routes' generateMetadata, but `getRequestConfig` ignored its `requestLocale` argument and unconditionally returned default-locale messages — so the server title was still ru, and (streamed late in dev) it kept overwriting the client's correct title: the exact symptom D19 was meant to kill. Fix: the config now honours `requestLocale` when a caller passes an explicit, known locale, and serves the default otherwise — no cookie/header is read in the config itself, so the landing (which never passes a locale) stays static (D6). Proven against the running dev server: `Cookie: ask.locale=en|kk` → `<title>Sign in - Ask</title>` / `<title>Кіру - Ask</title>`, h1 "Welcome back", `theme-dark aria-pressed="true"` server-rendered; no cookie → ru; landing → "Ask", static | src/shared/i18n/request.ts

2026-07-17 | **D19: platform preferences are cookie-mirrored — /app/* now server-renders the stored locale and theme (owner decision; kills the reload glitch).** The client-only preference model (D17/D18) surfaced three symptoms the owner rejected: on every /app reload the page painted the default (ru) and flipped to the stored language, the theme toggle flashed "system" before flipping to the stored option, and the tab title came up as the bare default until a client re-render. Root cause for all three: the server knew nothing about the stored preferences. Fix: `ask.locale`/`ask.theme` are mirrored to same-named cookies (written by `LocaleProvider`'s sync effect and `setThemePreference`/`ThemeSystemSync` respectively); the platform layout reads them (`await cookies()`) and seeds `LocaleProvider` (`initialLocale` — the SSR/hydration snapshot) and the new `ThemePreferenceSeed` context (exported from `shared/ui/theme-toggle`); the auth routes' `generateMetadata` is restored, now resolving the title from the cookie via `getTranslations({ locale })` — so the late-streamed dev title writes the SAME correct value and the race disappears. The client title effect stays for in-session switches. `LOCALE_STORAGE_KEY` moved to `shared/i18n/locales.ts` (a plain module) so server code imports it without pulling a client module (P6.2). Consequences: /app/* is dynamically rendered now (accepted — stateful chrome, not an SEO surface); the landing stays static (D6 lock intact); the (main) placeholder pages still server-render ru copy via the global config — they die with slices #2+. e2e: heading assertions restored to the real heading copy (the owner wants tab title ≠ on-page heading: `pageTitle` vs `title` keys), and the reload assertion now checks BOTH body and title survive in the stored language. Decision-log row D19 appended | src/app/app/layout.tsx, src/app/app/auth/{login,register}/page.tsx, src/shared/i18n/{LocaleProvider.tsx,locales.ts}, src/shared/theme.ts, src/shared/ui/{theme-toggle,theme-system-sync}.tsx, src/auth/ui/{LoginPage,RegisterPage}.tsx, e2e/auth.spec.ts, AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md §11 (D19)

2026-07-17 | **Tab-title fix corrected: the route metadata was overwriting the client sync on reload.** The 2026-07-16 title sync kept `generateMetadata` for the pre-hydration title — wrong: it is async (awaits `getTranslations`), so in dev its `<title>` streams in AFTER hydration and overwrote the client's localized `document.title` with ru on every reload; the title only recovered when the user switched language again (re-running the effect). Fix: the two auth routes now set NO title — the initial HTML carries only the static root default ("Ask"), and the client effect is the ONE writer of the tab title. Trade-off accepted: view-source/pre-hydration title is the neutral "Ask" instead of a ru page title — auth is not an SEO surface. (Owner refinement alongside: the tab uses dedicated `auth.*.pageTitle` keys — "Вход/Sign in/Кіру" — distinct from the on-page heading, and the suffix is "- Ask".) e2e: the language test now also reloads with the stored locale and asserts the title stays EN — the exact regression | src/app/app/auth/{login,register}/page.tsx, src/auth/ui/{LoginPage,RegisterPage}.tsx, e2e/auth.spec.ts

2026-07-16 | **Tab titles follow the client locale (closes the last audit item).** The auth routes' `generateMetadata` runs on the server, which is pinned to the default locale (D6 — no request-time cookie), so after a D18 client locale switch the page body changed language but the browser tab stayed ru. Fix (owner-chosen from three options): LoginPage/RegisterPage sync `document.title` in an effect keyed on the client locale — `"{title} — Ask"` — while `generateMetadata` stays for the pre-hydration/SEO title. Accepted trade-offs: a brief default-locale title before hydration, and crawlers index the ru title (fine for auth pages). Alongside: root metadata gains `title.template: "%s — Ask"` so server-rendered titles carry the brand suffix too (the landing keeps the bare "Ask" default); the e2e language test now also asserts `toHaveTitle("Welcome back — Ask")`. Rejected: fixed English titles (permanent language mismatch + an i18n-rule exception) and a locale cookie (forces `/app/auth/*` dynamic — machinery disproportionate to a tab title) | src/app/layout.tsx, src/auth/ui/{LoginPage,RegisterPage}.tsx, e2e/auth.spec.ts

2026-07-16 | **Audit fixes, round three: system-theme tracking everywhere + a stable error row.** (1) **A mid-session OS theme switch now re-resolves `data-theme` on EVERY page.** The `matchMedia("change")` listener was bound only inside `subscribeTheme` — i.e. only where a ThemeToggle happened to be mounted (auth pages only), so "system" mode froze elsewhere until a reload. The binding is now `ensureSystemThemeListener()` (exported, idempotent) and a headless client component `shared/ui/theme-system-sync` calls it once, mounted app-wide by `app/providers` (defined in shared, mounted by app — R3/P5.3). First paint is unchanged (the D17 pre-paint script). (2) **Field's reserved error line — tried and REVERTED the same day.** A permanently rendered `role="alert"` row (`min-h-5`) was added so an appearing validation error could not shift the form, then reverted on owner review: the ~26px of reserved space per field visibly broke the form's rhythm (screenshot-verified), and the compact spacing matters more than shift-on-error — which only occurs at submit time. Field renders the error conditionally, as before; the trade-off and the decision are recorded in a comment at the site | src/shared/theme.ts, src/shared/ui/theme-system-sync.tsx (new), src/app/providers/AppProviders.tsx, src/auth/ui/Field.tsx

2026-07-16 | **Audit fixes, round two: the LOW findings.** (1) **`<html lang>` now follows the client locale on mount, not only on switch** — an effect in `shared/i18n/LocaleProvider` syncs the attribute whenever the locale resolves (the DOM write moved out of `setStoredLocale`, ONE mechanism). Before: a reload with a stored kk/en rendered that language under the server's `lang="ru"`, misleading screen readers and hyphenation. (2) **`CardWithoutBorder` deleted** (was added to `shared/ui/card.tsx` at its FIRST consumer — the D8 rule of three promotes at the third — and its one use in AuthShell neutralized every style it added): AuthShell wraps the form in a plain `div` (P7.4). (3) **Mobile e2e project added** — `playwright.config.ts` runs every spec on `Pixel 7` as well as Desktop Chrome, so the mobile-first claims (§7 touch floor, the AuthShell phone layout) are proven, not asserted. (4) **e2e navigation locators decoupled from translated copy** (the same trap the ROADMAP parked fix names for the landing smoke test): the login→register cross-link keys on `href`; the theme buttons, the EN locale button and the role-modal customer button key on new stable `data-testid`s (`theme-*`, `locale-*`, `role-choose-*`); the copy ASSERTIONS in the language test stay — they are the point of that test. (5) **Unused `StartRoute` type deleted from `auth/model.ts`** (P8.1 — the mapper takes the backend's `string`; the value set is documented in `features/auth/contracts.md`). (6) **`public/logo_vertical.svg` kept but PARKED** in ROADMAP "Parked fixes" against item 10 (the landing, its expected first consumer) — not deleted, because the file is untracked (unrecoverable) and owner-authored; delete with the landing if it ships without it | src/shared/i18n/LocaleProvider.tsx, src/shared/ui/{card,theme-toggle,language-switcher}.tsx, src/auth/ui/{AuthShell,RoleSelectionModal}.tsx, src/auth/model.ts, playwright.config.ts, e2e/auth.spec.ts, AI_Knowledge/ROADMAP.md

2026-07-16 | **Audit fixes: the 44px touch floor + the last hardcoded string.** The 2026-07-16 audit found five controls under the platform-ui-design §7 touch-target floor (≥44px on the customer path) and one hardcoded string surviving in `shared/ui`. (1) **Theme toggle + language switcher** buttons: base size is now 44px, compacting to the original 32px only under Tailwind's `pointer-fine:` variant — mobile-first: the safe target is the default, the fine-pointer (mouse/trackpad) case is the refinement, and hybrid/touch devices are never under-served. (2) **VerifyCodeForm's "change email"** back button: `size="sm"` (32px) → `size="lg"` (44px), matching the submit above it. (3) **The agreement checkbox**: a `<label>` now wraps ONLY the 20px box (not the link-bearing sentence — that placement was deliberate and stands), growing the hit area to 44×44 while the Terms/Privacy links stay independently clickable; visual spacing unchanged (the label's centring replaces the old `gap-3`). (4) **Dialog close button**: was a bare 16px icon — now a 44px hit area positioned so the icon lands where it always did; and its screen-reader label `Закрыть` (hardcoded ru, pre-existing from Phase 0b but made a live bug by the D18 locale switch) is now `common.close` (ru/kk/en), following the pattern the theme toggle already set for shared/ui chrome copy | src/shared/ui/{theme-toggle,language-switcher,dialog}.tsx, src/auth/ui/{VerifyCodeForm,RegisterForm}.tsx, src/shared/i18n/messages/{ru,kk,en}.json

2026-07-16 | **Retroactive record of a CORE-file edit (flagged by the 2026-07-16 audit).** `DESIGN_PATTERNS_FRONTEND.md` P6.3's example was changed in place from `formType === "signup"` to `formType === "register"` when the auth pages were renamed to login/register — aligning the example with the product's real page names. The rule itself is untouched; only the illustrative string changed. Recorded here because the CORE files change by append + date + justification, never silently — the edit shipped without this record, which the audit caught. No further action; this entry IS the record | AI_Knowledge/DESIGN_PATTERNS_FRONTEND.md (P6.3 example only)

2026-07-15 | `.markdownlint.yaml` added — editor-only, no dependency and not in CI. Silences the seven rules the authored docs deliberately break (long single-line Changelog entries, lists under headings, quoted tag syntax, bare URLs, the `src/auth/*` glob false-positive, compact tables); everything else stays on. The alternative — reformatting tables and headings across the CORE authorities to satisfy an extension — is the silent rewrite those files forbid. YAML not `.jsonc`: Prettier appends a trailing comma to JSONC, which not every JSONC parser accepts | .markdownlint.yaml

2026-07-15 | **The build never type-checked `e2e/` — closed.** Chasing a "Cannot find name 'process'" squiggle in `playwright.config.ts` surfaced the real cause: `tsconfig.json`'s `include` listed only `next-env.d.ts`, `src/**` and `.next/types/**`, so `e2e/**` and `playwright.config.ts` were **outside the type program entirely** (`tsc --listFiles` counted 0 of them). Two consequences: the editor fell back to an inferred config with no `types: ["node"]` (hence the squiggle), and — the part that mattered — **a type error in a Playwright spec could never fail `npm run build` or CI**; it would only ever surface when the suite ran. §8's promise that the build gates types was quietly untrue for the e2e tree. Added `e2e/**/*.ts` + `playwright.config.ts` to `include`; `tsc` now covers 4 more files and passes, build + format:check green. No rule changed — this makes the existing §8 gate actually cover what it claimed | tsconfig.json

2026-07-15 | **Auth refined (owner feedback): standalone pages, password login, centered layout.** (1) **No app nav on auth** — the nav-bearing platform pages moved into a `(main)` route group with its own nav layout; `/app/auth/*` sits OUTSIDE it, so login/register render standalone while still inheriting the LocaleProvider (the switcher works). URLs unchanged (a route group adds no segment); the platform layout now mounts only the LocaleProvider. (2) **Log in is now email + password** (`POST /auth/login`) instead of the passwordless OTP: register still verifies the email with a 6-digit code, but returning users sign in with a password — the session is returned directly (or a 2FA challenge → the shared verify step). Dropped the unused `customer/login/start`; added `LoginRequest` + `api.login`; login shows "incorrect email or password" on 401. (3) **AuthShell relaid out** — a `max-w-md` card centred in the viewport holding one column: bigger wordmark on top (`logo_horizontal.svg` — `logo.svg` had been deleted, so the first cut pointed at a 404) → heading → the language + theme controls (moved to BETWEEN the heading and the card) → form → cross-link. **Rebuilt mobile-first** after review: base classes are the phone and `sm:` scales up; the outer wrapper holds `px-4` at every width so the card never touches the screen edge under `max-w-md`; `min-h-svh` (not `100vh`) centres it vertically without the mobile URL-bar jump, and as a *min* height the tall register form grows the wrapper instead of clipping. (4) **The agreement row** is now `"I agree to the <terms>Terms of Service</terms> and <privacy>Privacy Policy</privacy>"` via next-intl `t.rich`, so each locale places its own links; `text-balance` keeps the wrap tidy. It is deliberately NOT inside a `<label>` — a label toggles the checkbox when a link is clicked — so the checkbox carries its own `aria-label` (a link-free sentence). **Known gap, owner-accepted:** `/terms` and `/privacy` do not exist and 404 until the landing (roadmap #10) builds them — the legal copy is the owner's to write, never invented (P9.1); tracked in ROADMAP "Parked fixes". i18n: `actions.signIn`, `placeholders.passwordLogin`, `errors.{passwordRequired,invalidCredentials}` (ru/kk/en). Verified: build green (12 routes, auth + marketing static), **e2e 13/13** (login now email+password; a no-nav-on-auth assertion added), format:check clean, both themes × ru/kk/en driven | src/app/app/(main)/* (moved pages + new layout), src/app/app/layout.tsx, src/auth/{model,api,hooks}.ts, src/auth/ui/{LoginForm,AuthShell}.tsx, src/shared/i18n/messages/{ru,kk,en}.json, e2e/auth.spec.ts, AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md §2, AI_Knowledge/features/auth/{contracts,README,ux-ui-flow}.md

2026-07-15 | **Auth split into two pages + a theme toggle and language switch (owner request, two locked decisions re-opened with approval).** The tabbed single AuthPage became **two routes** — `/app/auth/login` (LoginPage) and `/app/auth/register` (RegisterPage), with `/app/auth` redirecting to login and a cross-link between them — like the UmitPath reference; a shared `AuthShell` (auth ui/) carries the logo (`public/logo.svg`), the controls, the heading and the card. The "Applicant" banner was never ours (nothing removed); confirm-password already existed. **Theme toggle (D17, reverses D13's "no toggle"):** light/dark is now chosen by a `data-theme` attribute on `<html>`, set before first paint by a tiny inline script in `app/layout.tsx` that resolves the stored `ask.theme` (or the OS, for "system"). The measured OKLCH dark palette moved from `@media (prefers-color-scheme: dark)` to `:root[data-theme="dark"]` — ONE copy, never duplicated, so the 31 verified contrast pairs stand; `color-scheme` moved to the per-theme blocks. `next-themes` stays refused (D14): the store is own-code `shared/theme.ts`, the control `shared/ui/theme-toggle` (light/dark/system, useSyncExternalStore). Components still never write `dark:`. **Language switch (D18, extends D6):** `shared/i18n/LocaleProvider` (client), mounted in the platform layout, nests inside the root NextIntlClientProvider and re-provides ru/kk/en messages (bundled client-side) for the stored `ask.locale` — a pure client re-render, NO per-request cookie read, so the static SEO marketing landing at `/` stays static (D6 preserved); the switch is scoped to `/app`. `shared/ui/language-switcher` (RU/KK/EN). NavigationMenu became a client component so the chrome follows the switch instead of freezing on the server locale. i18n: `auth.login`/`auth.register` groups replace the tab keys; a new `common` namespace holds the toggle labels (ru/kk/en). Verified: build green (12 routes, `/app/auth/*` + marketing all static), **e2e 13/13** (incl. new theme-attribute + ru→en switch + redirect + cross-link tests; design-system.spec still green — the pre-paint script sets data-theme from the emulated colorScheme), format:check clean, and both themes × ru/kk/en driven in a real browser (Kazakh ә/ң/ұ/ү/қ/ө/і resolve via Golos). No new npm dependency | src/design-system/tokens.css, src/app/globals.css, src/app/layout.tsx, src/app/app/layout.tsx, src/app/_components/NavigationMenu.tsx, src/shared/theme.ts, src/shared/ui/{theme-toggle,language-switcher}.tsx, src/shared/i18n/LocaleProvider.tsx, src/shared/i18n/messages/{ru,kk,en}.json, src/auth/ui/{AuthShell,LoginPage,RegisterPage}.tsx (AuthPage removed), src/auth/index.ts, src/app/app/auth/{page,login/page,register/page}.tsx, e2e/auth.spec.ts, AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md (§2/§7/D17/D18), AI_Knowledge/features/auth/*

2026-07-15 | **Phase 1 slice #1 — `auth` — built: the session foundation + the customer OTP auth flow.** Read the backend `identity` source (controller + DTOs + AuthProcessor/LoginProcessor), not just the summary docs, so no DTO was guessed. **Anatomy:** `model.ts` (request/response DTOs; `AuthUser` discriminated union customer/business/staff, P4.2; pure mappers `toAuthUser`/`roleToKind`/`startRouteToPath`, P5.1), `api.ts` (register/login-start/verify/session/logout on httpClient), `hooks.ts` (the `AuthContext` + `useAuth` + `useAuthState` + the sign-up/log-in/verify flow hooks), `ui/` (AuthPage with Log in/Sign up tabs, LoginForm, RegisterForm, shared VerifyCodeForm, RoleSelectionModal, AuthProvider, Field), `index.ts`. Thin server route renders the client AuthPage (D7); `app/providers` mounts `AuthProvider` + the `<Toaster>` (auth is its first real consumer, closing a Phase 0b deferral). **Decisions:** (a) customer auth is email + 6-digit code (OTP) — the vision's mechanism; the password login `/auth/login` + `select-role` and business/staff registration are a *different* flow, deferred to the seller path (roadmap #7), their response fields modelled but not exercised (P8/YAGNI). (b) **The role-choosing modal is the `suggestRoleExpansion` prompt** after a new signup — decided WITH the owner over two other backend candidates (`requiresRoleSelection`, a client intent fork); it maps to a real backend signal, not invented UI (P9.4). Shown from the auth flow in V1 (search Home doesn't exist yet) and exported so Home hosts the trigger when `search` lands. (c) **Session state is a zustand store factory consumed via a context provider (D7)** — `createAuthStore()` (store.ts, pure state + actions, DOM-free per D5), one instance per `AuthProvider` (never a module-scope singleton), read through `useAuth` with selector subscriptions; token-storage + API side effects (`applySessionTo`, `signOut`, session restore) are orchestrated in hooks.ts. zustand (5.0.14) was installed for this at the owner's explicit request — decision-logged since D2, this is its first real consumer. (An interim context+useReducer version was built first when zustand was absent and installs were gated, then refactored to the store factory once the install was approved.) **Contract drift corrected** in `features/auth/contracts.md`: dropped `phone` from AuthUserResponse (backend removed it V8), completed AuthSessionResponse (activationRequired/requiresRoleSelection/availableRoles/allRoles/requiresTwoFactor/authChallengeId/suggestRoleExpansion), noted AuthChallengeResponse.code is test-mode only, added request DTO shapes + the consumed-vs-deferred split. **R4 teeth landed alongside** (ROADMAP parked fix): a small custom flat-config ESLint rule `local/no-cross-element-relative-import` catches a relative import that escapes its element to a *legal* target (invisible to `boundaries/dependencies`, which classifies by resolved path); proven by `lint-fixtures/src/auth/bad-r4-relative-escape.ts` + a clean within-slice fixture, both registered in `check-lint-fixtures.mjs`. i18n `auth` namespace added in ru/kk/en (real strings). e2e `auth.spec.ts` drives the real screens against `page.route`-stubbed endpoints (no live backend). Verified: `eslint src`, `lint:fixtures` (all 7 laws proven incl. R4), `tsc`, `next build`, `format:check`, e2e 10/10 green, both themes driven in a real browser | package.json + package-lock.json (zustand 5.0.14), src/auth/* (model, api, store, hooks, index, ui/*), src/app/app/auth/page.tsx, src/app/providers/AppProviders.tsx, src/shared/i18n/messages/{ru,kk,en}.json, eslint.config.mjs, scripts/check-lint-fixtures.mjs, lint-fixtures/src/{auth,shared}/*, lint-fixtures/README.md, e2e/auth.spec.ts, AI_Knowledge/features/auth/*, AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md §4, AI_Knowledge/ROADMAP.md

2026-07-15 | Brand marks placed and reconciled to the accent. Two SVGs (`favicon.svg`, `logo.svg`) had sat untracked at the repo root — wrong under §2, and painted in `#E37627`, a THIRD orange from the prior Vite frontend matching neither accent token. **Favicon → `src/app/icon.svg`** (renamed): the App Router auto-detects that metadata name and injects `<link rel="icon" type="image/svg+xml">` — verified in the rendered head, zero hand-wiring. A file named `favicon.svg` would NOT be auto-wired (only `favicon.ico`/`icon.*` are). **Logo → `public/logo.svg`** (created `public/`), a static asset served at `/logo.svg`. Both recoloured `#E37627` → `#c25202`, the canonical (light) accent `oklch(0.575 0.161 46)` — a favicon can't reliably be theme-aware (Chrome ignores in-SVG `prefers-color-scheme` for tab icons), so one fixed accent value is correct; the upgrade path for a logo that brightens on dark is an inline component using `text-accent`. §2 refined by a dated note (favicon is an `app/` metadata file, not a `public/` asset) + the tree updated. Build green; `/icon.svg` and `/logo.svg` both serve 200 | src/app/icon.svg (was ./favicon.svg), public/logo.svg (was ./logo.svg), AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md §2

2026-07-15 | **Vercel wired (D16) + Prettier adopted (D15) — Phase 0 is now complete** bar one human dashboard click. **Vercel:** the repo had no formatter and the deploy host was unset; the prior Vite frontend used Docker+nginx+Vercel, but Next.js builds natively on Vercel so no Docker/nginx. `vercel.json` sets `framework: nextjs` and pins `buildCommand: npm run build` — the whole point: Vercel's default bare `next build` skips `npm run lint && npm run lint:fixtures`, so without the override the deploy would run UNGUARDED while CI stayed green. `engines.node: 22.x` added because Vercel reads engines, not `.nvmrc`. Remaining: connect the repo in the Vercel dashboard (human; gives per-PR preview deploys). Dropped the `$schema` line from vercel.json — VS Code blocks the remote fetch as "untrusted" and Vercel ignores the key; not worth a per-machine trust prompt. **Prettier:** `prettier` + `prettier-plugin-tailwindcss` (canonical Tailwind class order). One formatter per §7; ESLint is boundaries-only so they don't fight — no `eslint-config-prettier` needed (nothing to disable). `format` / `format:check` scripts; `format:check` now gates the CI build job (fail-fast before the heavier build). Formatted 21 files: the shadcn scaffolds had no semicolons while the hand-written code did — Prettier normalized to the codebase's own style (semi, double-quote, trailing-comma-all, LF). `.gitattributes` pins `* text=auto eol=lf` so a Windows working copy and the Linux CI runner agree (otherwise `format:check` fails in CI on invisible CRLF); `.editorconfig` carries the universal basics. `.prettierignore` excludes what a tool must not rewrite: `.claude/` (vendored kept pristine + authored skills), `AI_Knowledge/` + `CLAUDE.md` + `AGENTS.md` (normative docs change by human append, never silent rewrite), `src/design-system/` (the D3 visual source, with its WCAG table). Verified after formatting: `format:check` clean, full build green (9 routes), e2e 5/5 — the class-sorting reorder is behavior-preserving | vercel.json, package.json (engines, format scripts, prettier devDeps), package-lock.json, .prettierrc.json, .prettierignore, .editorconfig, .gitattributes, .github/workflows/ci.yml (format:check step), 21 formatted source/config files, AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md (§7 + D15/D16), AI_Knowledge/ROADMAP.md

2026-07-15 | **CI landed — the last buildable box of Phase 0a.** `.github/workflows/ci.yml` (GitHub Actions; the repo is `Siirio/AskFrontend`). This closes the §8 gap: the ESLint boundaries were compile-time laws that only ran where someone chose to build — now an import/boundary violation, a stale rule fixture, a type error, or a build break fails the PR itself. Two jobs on push (master/dev/new_frontend) + every PR: **build** = `npm ci` → `npm run build` (eslint src → the lint:fixtures proof → tsc → next build); **e2e** = install chromium → `npm run test:e2e`. GitHub sets `CI=true`, which finally activates the CI branch already written into `playwright.config.ts` (forbidOnly, retries=2, reuseExistingServer=false) — those settings existed since 0a but had never run; traces upload on failure only. Node pinned once in `.nvmrc` (=22), read via `node-version-file` so the workflow and local `nvm use` share one source. Least-privilege `permissions: contents: read`; `concurrency` cancels superseded runs. Validated locally: `npm ci --dry-run` exits 0 (lockfile ⟷ package.json in sync, so the clean install will pass), the YAML parses, `.github/` is tracked (not gitignored). No new *decision* — §8 already MANDATED CI; this is its execution. **Still open, and deliberately not guessed: the deploy target** (Vercel/Netlify/self-host is a human call). Noted trap for whoever picks it: on Vercel, pin `buildCommand` to `npm run build` or the default bare `next build` skips the lint+fixture chain, passing CI while deploying unguarded | .github/workflows/ci.yml, .nvmrc, AI_Knowledge/ROADMAP.md, AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md §8

2026-07-15 | **Phase 0b COMPLETE (D14): the animation wrapper + the 9 `shared/ui` primitives.** GSAP installed (`gsap`, `@gsap/react`); `shared/motion.ts` is the single door — registers plugins, reads the design-system motion tokens (`--duration-*`, `--ease-*`) as GSAP defaults so CSS transitions and GSAP share ONE set of values (P6.2), and gates reduced motion once via `gsap.matchMedia()` (a CustomEase is built from `--ease-out` so the JS curve equals the CSS curve). GSAP never runs on the server (D7). Nine primitives scaffolded via shadcn CLI and restyled to tokens before first use (D12): Button, Input, Select, Card, Dialog (Modal, D10), Badge, Skeleton (own-code Spinner alongside), EmptyState (own-code), Toast (sonner). **The restyle was NOT a find-replace** — shadcn's `accent` is a subtle hover grey but ours is the brand orange, so a mechanical port would have painted orange on every ghost/outline hover, breaking the saturation-is-action lock; quiet variants hover to `surface-sunken`. Badge got ASK's rules: `rounded-xs` rectangle never a pill, an `offer` tint variant (chroma 0.032, weight from tabular numerals) and a `metadata` variant (default) that reads as fact — deliberately NO green/amber/red status variant (a coloured light is a rating, and ASK has none). Two dependency calls: **refused `next-themes`** (shadcn's sonner wrapper wants it, but ASK has no theme provider — light/dark is pure CSS `prefers-color-scheme`, no toggle, P9.1 — so the Toaster is own-code with `theme="system"`); **refused `tw-animate-css`** (stripped the dead enter/exit classes rather than run a second motion system beside GSAP, §7). Added: `components.json` (hand-written so the CLI never rewrites `globals.css` — our tokens stay the one source, proven by `--dry-run --diff globals.css`), a `--overlay` scrim token (warm, dark in BOTH themes — a modal scrim must not lighten in dark), and one `@utility focus-ring` (the 2px/2px/no-blur signature, defined once, applied on every focusable primitive). `lib/utils.ts` `cn()` on clsx+tailwind-merge; `class-variance-authority` for variant maps (P2.1); `lucide-react` icons. Full build green (9 routes, static/dynamic split intact); e2e 5/5; both themes screenshot-verified on a throwaway preview page (built, shot, deleted — never committed, P9.1). Deferred to a real consumer: mounting `<Toaster>` in providers, and `shared/ui` GSAP-driven micro-motion (no consumer yet, YAGNI) | package.json (gsap, @gsap/react, sonner, cva, clsx, tailwind-merge, lucide-react, radix-ui), components.json, src/lib/utils.ts, src/shared/motion.ts, src/shared/ui/*.tsx (10 files), src/design-system/tokens.css (--overlay), src/app/globals.css (focus-ring), AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md D14, AI_Knowledge/ROADMAP.md, CLAUDE.md/AGENTS.md stack line

2026-07-15 | **Phase 0b: the design tokens landed (D13) — `design-system/tokens.css` is the visual authority.** Also discovered that D3 chose Tailwind v4 but nobody ever installed it: no `tailwindcss` in package.json, no postcss config, `globals.css` empty. Added the deps + `postcss.config.mjs` (no new decision — D3 already made it). Authored in OKLCH so ramps are perceptually honest. The three problems the inherited direction left OPEN are closed **by measurement**: (a) **the accent** `oklch(0.575 0.161 46)` — hue 46 chosen deliberately between Amazon's yellow-orange (63) and the dusty terracotta of the AI-design cliché; chroma 0.161 is the sRGB gamut ceiling there (0.19 was out of gamut and would have been silently clipped); lightness 0.575 is the BRIGHTEST orange that still holds white text at AA (4.67:1) — one step brighter and the label fails, which is why the accent is deep: contrast chose it, not taste. Dark carries a *different* accent value at the *same* hue because one value provably cannot serve both (the light accent measures 3.84:1 on the dark surface — legal but muddy), so `--accent-foreground` is a token: white in light, ink in dark. (b) **the discount collision solved by REGISTER, not hue — "saturation is action, tint is information"** (now a lock): the accent is the only high-chroma fill and marks only actionable things; a Unique Offer is a low-chroma tint (chroma 0.032 vs 0.161) whose weight comes from bold tabular numerals. They cannot compete because they are not in the same register — and every marketplace scores discounts in red, where ASK states them as a fact. (c) **the typeface is Golos Text** (Cyrillic-native, drawn for a high-traffic public-service UI where people finish a task and leave — literally our mission). **Non-obvious find, now a lock:** ₸ the TENGE SIGN (U+20B8) lives in the `latin-ext` subset and in NO Cyrillic range — a ru/kk product that naturally loads only cyrillic+cyrillic-ext would silently fall every price back to a system font. Verified against the Google Fonts API, not assumed. Two more things the math caught: `success` was out of sRGB gamut and was clamped; `foreground-muted` sat only 0.03 lightness from the AA-floored `foreground-subtle` — three text levels where two are indistinguishable is a fake hierarchy, so muted was pushed to 0.45. **31/31 contrast pairs pass, 0 out of gamut.** Components never write `dark:` for colour (`@theme inline` resolves utilities to primitives the dark media query redefines); no theme toggle, because the vision has no such control (P9.1). **BUILD-VERIFIED, same day** (owner approved the install): `npm run build` green end to end — lint → boundary fixtures (all 5 still fail as designed) → tsc → 9 routes with the static/dynamic split intact. Driven in a real browser in both grounds: the body paints from the tokens, `--accent-foreground` genuinely flips white→ink, and ₸ + the Kazakh letters resolve from Golos rather than falling back (proven by forcing the lazy subset load — `document.fonts.check()` returns a FALSE NEGATIVE on a page that contains no ₸, because unicode-range subsets are only fetched when a glyph in range is painted; the width against a no-webfont control confirms it independently). **The two new Design Locks now have TEETH:** `e2e/design-system.spec.ts` asserts the *wiring*, not the values — body renders on the surface token, dark redefines it, the accent-foreground differs across grounds, the webfont covers ₸. A token may be retuned freely; the system cannot be silently unwired. Suite 5/5 | src/design-system/tokens.css, src/app/globals.css, src/app/layout.tsx, postcss.config.mjs, package.json, package-lock.json, e2e/design-system.spec.ts, AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md D13, AI_Knowledge/Locks.md, AI_Knowledge/ROADMAP.md, .claude/skills/{marketing,platform}-ui-design/SKILL.md

2026-07-15 | UI/UX workflow made real. FOUR problems fixed, all discovered by audit. (1) **Skills were unloadable:** they lived as flat `.claude/skills/{name}.md` files, but the harness only loads `.claude/skills/{name}/SKILL.md` with YAML frontmatter — so the Tool Routing table had been routing to skills that could never fire. Migrated; all 9 now load. (2) **The two design skills did not exist.** The 2026-07-14 entry below records them as authored, but they were never committed and were absent from the working tree — meanwhile DESIGN_BRIEF.md had been deleted, so the entire visual direction survived only in git history. Recovered from `git show` and split by audience: operational rules → the two skills; invariants → Locks.md. The claude.ai/design prompt pack (brief §9) stays deleted — git history keeps it if ever needed. (3) **The direction had no teeth.** Three Design Locks added: no marketplace idioms (no ratings/buy-box/cart/match-score, never a price-first default); quiet chrome + ONE orange accent on warm neutrals; trust badges render as metadata, never as a rating. An agent may improve the execution, never re-open the decision — the marketplace is the pattern the training data reaches for by default, so it must be a law. (4) **Third-party skills are VENDORED, not installed:** shadcn (MIT; CLI-driven — needs no MCP) and 3 of GSAP's 8 official skills (core, scrolltrigger, react; MIT), pinned to upstream commits and committed. Rationale: a committed skill inherits on a plain clone with zero bootstrap, and the version we build against is reviewable in the diff — where a per-machine install is a step that can (and on this machine, does) fail. Vendored files stay pristine; ASK's overrides live in the two design skills, which route to them (`.claude/skills/VENDORED.md`). Also: the UI work loop (invoke skill → build → verify visually → definition-of-done → code-rules-checker → docs) recorded in CLAUDE.md/AGENTS.md, and the Phase 0b token item sharpened — the three problems the inherited direction leaves OPEN (an AA-compliant orange with a stated foreground; an offer-label colour that does not collide with it; a typeface verified for Kazakh Cyrillic) are solved when the tokens land, not deferred. No install, no CLI run — vendoring is `curl` into the repo | .claude/skills/** (9 skills + VENDORED.md), CLAUDE.md, AGENTS.md, AI_Knowledge/Locks.md, AI_Knowledge/ROADMAP.md
2026-07-14 | D11 un-parked: GSAP accepted as the JS animation system (useGSAP() + refs, ScrollTrigger for scroll storytelling, shared.motion.ts utilities). Locked rule: animate transform/opacity only, never width/height/top/left (layout-recalculation cost). D12 opened and accepted: shadcn CLI adopted as SCAFFOLDING for shared/ui primitives — copied into the repo and restyled to design-system tokens before first use, Radix supplies behavior only, never a shipped default value (P9.2 unrelaxed). Two new project skills authored to carry this direction: .claude/skills/marketing-ui-design.md (app/(marketing)/, GSAP scroll storytelling, DESIGN_BRIEF-derived anti-generic rules) and .claude/skills/platform-ui-design.md (/app/*, shadcn-scaffolded primitive reuse, business-cabinet information-architecture-first sequencing). No npm install run — dependency install deferred to an explicit user "run"/"build" per CLAUDE.md runtime constraints | AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md D11/D12/§2/§7, AI_Knowledge/Locks.md, AI_Knowledge/ROADMAP.md, AI_Knowledge/DESIGN_PATTERNS_FRONTEND.md P3.1, CLAUDE.md, AGENTS.md, .claude/skills/marketing-ui-design.md, .claude/skills/platform-ui-design.md
2026-07-14 | framer-motion removed at owner request (D11 reverses D4). It was never installed — zero deps in package.json/lock, zero source, shared/motion.ts never created — so this is a decision-only change with no code impact. §7's JS-animation row is now OPEN/parked: no JS animation library installed; CSS transitions/@keyframes on design-system motion tokens (reduced motion via prefers-reduced-motion) are the interim mechanism. A JS library — GSAP the likely candidate — enters only via a future decision-log row decided WITH the design; GSAP is imperative (useGSAP + refs), so no library-specific shape was committed in the docs in advance. Updated: CLAUDE.md/AGENTS.md stack line, architecture §2 tree + §3 'use client' policy + §7 table + D2/D4/D11 log, DESIGN_PATTERNS P3.1 example, ROADMAP Phase 0b + Phase 3 items. (DESIGN_BRIEF.md removed separately, by owner, same session — not part of this edit) | CLAUDE.md, AGENTS.md, AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md, AI_Knowledge/DESIGN_PATTERNS_FRONTEND.md, AI_Knowledge/ROADMAP.md
2026-07-14 | R4 clarified by dated §4 note (audit follow-up): "slice" reads as "element" — relative imports legal only within one element (slice, app/, or one toolbox folder), crossing elements always via @/. Only the legal-target relative escape lacks ESLint teeth (illegal targets are caught by resolved-path classification); tooling parked to Phase 1 slice #1, tracked in ROADMAP "Parked fixes" with the landing-test locator fix. CI check folded into the open 0a deploy checkbox | AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md §4, AI_Knowledge/ROADMAP.md
2026-07-14 | i18n plumbing landed: next-intl (D2, §7) with messages/{ru,kk,en}.json keyed per slice namespace; request config in shared/i18n (sanctioned §5 exception). Locale is FIXED to defaultLocale "ru" until the profile settings screen (UF 2.3, Phase 1) provides a real selector — reading a cookie per request would force dynamic rendering and break the static SEO-first marketing landing (D6). Revisit when the profile slice lands | src/shared/i18n/*, next.config.ts
2026-07-14 | Transport layer landed: httpClient on plain fetch (callable server+client, D7), ApiError mirroring the backend's ErrorResponse (timestamp/errorCode/message/detail/errors[]), token access behind the TokenStorage interface with the ask.accessToken key (P5.2, D6), env reads in shared/config (default API base http://localhost:2020 — backend ASK_SERVER_PORT). NO key-transform layer: the backend speaks camelCase JSON (kz.ask.shared.api.dto), so the roadmap's "key transforms" item is satisfied by JSON (de)serialization alone — a case-mapping layer would be dead code (P8.1) | src/shared/api/*, src/shared/config/env.ts
2026-07-14 | Enforcement scaffold born under eslint-plugin-boundaries v7 + TypeScript 6: §8's element-types/entry-point are folded into the canonical boundaries/dependencies rule (R2 expressed via fileInternalPath: "index.ts"), no-unknown is now no-unknown-dependencies, tsconfig uses paths without the removed baseUrl. Laws R1–R5 unchanged; proven by lint-fixtures/ + scripts/check-lint-fixtures.mjs, a mandatory stage of npm run build. Dated correction appended to architecture §8 | eslint.config.mjs, lint-fixtures/*, scripts/check-lint-fixtures.mjs, tsconfig.json, AI_Knowledge/ARCHITECTURE_PATTERN_FRONTEND.md

## 2026-07-14 | ROADMAP moved into the repo (was an untracked orphan at the workspace root — outside every git repo, lost on any fresh clone) and rewritten greenfield: no salvage/archive narrative, no "the Design" authority, phases and gates rebuilt around the 8 real slices. It is the 4th always-loaded file — the vision says WHAT, the architecture WHERE, the design patterns HOW, the roadmap WHEN. Three real gates opened: G1 search sort/filter contract (UnifiedSearchRequest takes only query/cityId/limit but the vision promises distance/cost/offers sorting + price/companies/location filters), G2 Company Profile scope, G3 what "Proceed to Purchase" actually does (likely the backend contact module — would need a new slice). Completed phases now retire into this Changelog | AI_Knowledge/ROADMAP.md, CLAUDE.md, AGENTS.md, ARCHITECTURE_PATTERN_FRONTEND.md, .claude/skills/system-maintainer.md
2026-07-14 | The 3 CORE files moved from the repo root into AI_Knowledge/ — ONE knowledge home. Root now holds protocol only (CLAUDE.md/AGENTS.md), mirroring the backend's split. Names kept (they stay siblings, so their cross-references still resolve). Restores the location ROADMAP.md already pointed at | AI_Knowledge/{PRODUCT_VISION,ARCHITECTURE_PATTERN_FRONTEND,DESIGN_PATTERNS_FRONTEND}.md, CLAUDE.md, AGENTS.md, .claude/skills/*
2026-07-14 | Backend cross-repo lookups fixed: its Feature Index gained a Frontend slice column (messaging→chats, service→services, request→requests, identity→auth+profile, offers/import→business-cabinet/catalog); paths corrected to ../Ask_Frontend | ../Ask_Backend/CLAUDE.md, ../Ask_Backend/AGENTS.md
2026-07-14 | AI knowledge system installed, mirroring Ask_Backend: self-bootstrapping CLAUDE.md/AGENTS.md orchestrator, .claude/skills (code-rules-checker, documentation-updater, system-maintainer), AI_Knowledge/{Locks,Changelog,features}. The CORE files are compress-exempt — the maintainer protocol may never rewrite them | CLAUDE.md, AGENTS.md, .claude/skills/*, AI_Knowledge/*
2026-07-14 | Feature folders == slices, 1:1 (8 slices mirroring backend modules identity/search/catalog/service/chat/request/business+offers). autodump, contact, shipping have no V1 surface → no slice. Marketing is app-level wiring, not a slice | AI_Knowledge/features/*, CLAUDE.md Feature Index
2026-07-14 | Greenfield rebuild from two sources of truth: PRODUCT_VISION.md (product authority, D9) + the AskBackend API (data authority). VSA on Next.js App Router with ESLint-enforced boundaries; decisions D1–D10 | ARCHITECTURE_PATTERN_FRONTEND.md, DESIGN_PATTERNS_FRONTEND.md, PRODUCT_VISION.md
